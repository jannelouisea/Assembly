.context \@cpu
.context cpu
.context @at_CPU
.topic CPU Type Equate
.freeze 3
..index @Cpu equate
 \i\p\aExample\v@Pushc.asx\v\i\p                                   \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @Cpu

  \bSee also:\p PUSHCONTEXT, \aProcessor Selection Directives\v@ProcSelect\v

  \bDescription:\p

     Predefined equate returning the type of processor selected by the
     previous processor directive (8086 is the default). Bits are set
     for each group of instructions supported by the processor. For
     example, when the .286 and .287 directives are in effect, bits 0,
     1, 2, 8, and 10 will be set.

     The .486 directive sets bits in @Cpu as if the .387 directive were
     used.

       15                          8    7                           0
     ÚÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄ¿ÚÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄÂÄÄÄ¿
     ³   ³   ³   ³   ³387³287³   ³87 ³³Prv³   ³   ³486³386³286³186³86 ³
     ³   ³   ³   ³   ³   ³   ³   ³   ³³   ³   ³   ³   ³   ³   ³188³88 ³
     ÀÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÙÀÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÁÄÄÄÙ

     \bBit\p     \bProcessor Compatibility\p

      0      8086/8088
      1      80186/80188
      2      80286
      3      80386
      4      80486
      7      Privileged instructions enabled
      8      8087
     10      80287
     11      80387

     All other bits are reserved, and their values are undefined.
.context \@CODE
.context @at_CODE
.topic Code Segment Text Macro
.freeze 3
..index @code text macro, Code-Segment Name Text Macro
 \i\p\aExample\v@Vector.asx\v\i\p                                   \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @code

  \bSee also:\p @CodeSize, @data, .CODE, \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Text macro representing the segment name defined by .CODE. Can be
     used in ASSUME statements or any place a segment name would be
     valid. Under the TINY memory model, @code expands to DGROUP.
.context \@CodeSize
.context CodeSize
.context @at_CodeSize
.topic Code Size Equate
.freeze 3
..index @CodeSize equate
 \i\p\aExample\v@vector.asx\v\i\p                                   \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @CodeSize

  \bSee also:\p @DataSize, .MODEL, .CODE, \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Predefined equate giving a value of 0 for tiny, small, compact, and
     flat models, or 1 for medium, large, and huge models. The .MODEL
     directive must precede references to this equate. Useful in
     conditional assembly.
.context \@CurSeg
.context CurSeg
.context @at_CurSeg
..index @CurSeg text macro
.freeze 3
.topic Current Segment Name Macro
 \i\p\aExample\v@Global.asx\v\i\p                                   \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @CurSeg

  \bSee also:\p ENDS, MACRO, .DATA, .CODE

  \bDescription:\p

     Text macro returning the name of the current segment. Useful for
     ASSUME statements and segment overrides (especially inside
     macros), as well as for ending the current segment when mixing
     simplified and full segments. @CurSeg allows you to terminate
     segments started in include files so that the segment will close
     before the next include. Returns <> (blank) when not in a segment.
.context \@DATA
.context @at_DATA
..index @data macro, DATA/DATA?/CONST/STACK Location Macro
.freeze 3
.topic Pointer to DATA/DATA?/CONST/STACK
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @data

  \bSee also:\p .DATA, .CONST, .STACK, .STARTUP, .MODEL, \a.DATA?\v@.DATA\v,
            @DataSize, \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Predefined equate representing DGROUP, which contains all near
     data segments that are defined by the .DATA, .DATA?, .CONST, and
     .STACK directives. To access data in any of these segments, DS
     must contain the value @data (under DOS, the .STARTUP directive
     automatically initializes DS). This equate can be used with the
     ASSUME directive or other directives that require segment names.

     The .MODEL directive must precede this equate.
.context \@DataSize
.context DataSize
.context @at_DataSize
..index @DataSize equate
.freeze 3
.topic Data Size Equate
 \i\p\aExample\v@hellom.asx\v\i\p                                   \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @DataSize

  \bSee also:\p @CodeSize, .MODEL, \a.DATA\v@.DATA\v, \a.DATA?\v@.DATA\v, \aConditional Assembly\v@CondAssem\v,
            \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Predefined equate giving a value of 0 for tiny, small, medium, and
     flat models, 1 for compact and large models, and 2 for huge model.
     This equate is often used in conditional assembly blocks.

     The .MODEL directive must precede this directive.
.context \@Time
.context Time
.context Date
.context \@Date
.context @at_Date
..index @Date, @Date Text Macro
..index clock,, accessing, @Time Text Macro
..index clock,, accessing, @Date Text Macro
..index @Date text macro, Current Date/Time Text Macros
..index @Time text macro, Current Date/Time Text Macros
.freeze 3
.topic Current Date and Time Text Macros
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @Date

            @Time

  \bSee also:\p ECHO

  \bDescription:\p

     @Date is a text macro returning the system date at the time of
     assembly in mm/dd/yy format. The @Time text macro returns the system
     time at assembly in hh:mm:ss 24-hour format. These macros are useful
     for time-stamping assembler output.
.context \@Environ
.context Environ
.context @at_Environ
..index @Environment macro
.freeze 3
.topic Environment Variable Macro
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @Environ(envvar)

  \bDescription:\p

     Returns the value of environment variable <envvar>. Returns a
     blank if <envvar> is not defined.
.context \@FarData
.context \@FarData?
.context @at_FarData
.context @at_FarData?
..index @fardata? macro, Undefined Far Data Location Macro
..index @fardata macro, Far Data Location Macro
.freeze 3
.topic Far Data Location Macro
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @fardata

            @fardata?

  \bSee also:\p .FARDATA, \a.FARDATA?\v@.FARDATA\v, @data, @DataSize, .CONST,
            .STACK, .MODEL, SEG, \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Predefined text macro returning the name of the segment defined by
     the last .FARDATA or .FARDATA? directive. This macro normally
     equals FAR_DATA or FAR_BSS.
.context \@FileCur
.context \@FileName
.context FileCur
.context FileName
.context @at_FileCur
..index @FileCur macro, Current Source Filename Macro
..index @FileName macro, Current Base Source Filename Macro
.freeze 3
.topic Current Source Filename Macros
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @FileCur

            @FileName

  \bSee also:\p ECHO

  \bDescription:\p

     @FileCur is a text macro returning the name of the current source
     or include file, including any necessary relative or absolute path.
     The @FileName text macro returns the base name of the current
     assembly file; if in an include file, it returns the name of the
     calling source file. This macro is useful for any situation in which
     you want a name to change whenever the name of the file or include
     file changes.
.context \@Interface
.context Interface
.context @at_Interface
..index @Interface macro
.freeze 3
.topic Current Source Filename Macro
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @Interface

  \bSee also:\p .MODEL, \aOPTION LANGUAGE:\v@LANG\v

  \bDescription:\p

     Predefined equate returning a byte-size bit flag describing the
     current language and operating-system types.

       \bBits 0-2          Bit 7\p

       000   Reserved     0   MS-DOS or Windows
       001   C            1   OS/2
       010   SYSCALL
       011   STDCALL
       100   Pascal
       101   FORTRAN
       110   Basic
       111   Reserved

     All other bits are reserved for future use, and their values are
     undefined.
.context \@Line
.context Line
.context @at_Line
.context LINE
..index @Line macro
.freeze 3
.topic Current Line-Number Macro
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @Line

  \bSee also:\p .ERR, ECHO

  \bDescription:\p

     Predefined equate returning the current line number in the module.
     If in an include file, returns the line number relative to the
     include file. If in a macro, returns the line number relative to
     the beginning of the source file. Useful for showing the line
     number of a programmer-defined error.

  \bExample:\p

     IFNDEF   Country
              .ERR       <Country code expected at line @Line>
     ENDIF
.context \@MODEL
.context @at_MODEL
.topic Model Equate
.freeze 3
..index @Model directive
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @Model

  \bSee also:\p \aTable of Memory Models\v@Memorymodels\v, .MODEL, @CodeSize, @DataSize,
            @Interface

  \bDescription:\p

     A predefined equate that returns one of the following values,
     depending on memory model previously defined with the .MODEL
     directive:

      \bValue\p   \bMemory model\p

        1     TINY
        2     SMALL
        3     COMPACT
        4     MEDIUM
        5     LARGE
        6     HUGE
        7     FLAT

     The @Model equate is the only way of identifying the TINY and FLAT
     memory models. This equate is often used in conditional assembly
     blocks. The @Model equate is defined only after a .MODEL directive.
.context \@Stack
.context @at_Stack
..index @stack macro
.freeze 3
.topic Stack Location Macro
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @stack

  \bSee also:\p .MODEL, .DATA, @data, .STACK, \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Predefined equate returning the name of the group containing the
     stack segment. This equate expands to DGROUP for NEARSTACK and to
     STACK for FARSTACK, as defined in a preceding .MODEL statement.
.context \@Version
.context Version
.context @at_Version
..index @Version equate
.freeze 3
.topic Assembler Version Equate
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @Version

  \bDescription:\p

     Prefined equate returning a three-digit string giving the current
     version number of the assembler. Returns 610 for this version
     of the assembler, 520 for QuickAssembler, and 510 for MASM 5.1. It
     is undefined for MASM version 5.0 and earlier.
.context \@WordSize
.context WordSize
.context @at_WordSize
..index @WordSize equate
.freeze 3
.topic Word Size Equate
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @WordSize

  \bSee also:\p \aOPTION SEGMENT:\v@optseg\v, \aFLAT Memory Model\v@Flat\v, .386, .486

  \bDescription:\p

     Predefined equate returning the size of a word. In default 16-bit
     mode, @WordSize returns 2. On 80386/486 processors in a 32-bit
     segment (such as the FLAT model), @WordSize returns 4.

     Outside of a segment this equate returns the default word size as
     set by a processor selection directive or the OPTION SEGMENT:
     directive.
.context ALIAS
.freeze 3
.topic RESERVED WORD
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     The ALIAS keyword is reserved for future use.
.context @ALIGN
.context EVEN
.context ALIGN
..index ALIGN directive
..index EVEN directive
.freeze 3
.topic Align Data and Code
                                             \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   ALIGN [number]

            EVEN

  \bSee also:\p ORG, SEGMENT, STRUCT, \aData Declaration Directives\v@byte\v

  \bDescription:\p

     ALIGN aligns the next variable or instruction on an offset address
     that is a multiple of <expression>. The <number> parameter must be
     a power of 2 (1,2,4,8,...) less than or equal to the alignment of
     the current segment. If <number> is omitted, the alignment is
     determined by the align field of the preceding SEGMENT directive.

     In a segment containing no instructions, the assembler pads each
     skipped byte with nulls (00h). In a code segment, the assembler
     inserts a no-operation sequence (not necessarily NOP instructions)
     to fill the gap.

     This directive is useful for optimizing data and instruction
     fetches on processors with 16- or 32-bit data paths.

     EVEN is a synonym for ALIGN 2.
.context @.ALPHA
.context .ALPHA
.context ALPHA
.context DOSSEG
.context .DOSSEG
.context SEQ
.context .SEQ
..index .ALPHA directive
..index .SEQ directive
..index .DOSSEG directive
.topic Define Segment Ordering
.freeze 3
 \i\p\aExample\v@Hello.asm\v\i\p                                   \i\p\aUp\v@CompSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p  .SEQ

           .DOSSEG

           .ALPHA

  \bSee also:\p SEGMENT

  \bDescription:\p

     The .SEQ directive outputs segments to the object file in the same
     order they appear in the source code. The .ALPHA directive outputs
     segments to the object file in alphabetical order by segment name.
     The .DOSSEG directive orders segments according to the standard
     DOS convention used by other Microsoft languages:

        ş CODE segments
        ş Other non-DGROUP segments
        ş DGROUP segments
             ş Segments not in BSS or STACK
             ş BSS class segments
             ş STACK class segments

     When other Microsoft languages are linked with MASM modules,
     or if the .MODEL directive is used, linker automatically uses
     the .DOSSEG ordering.

     The .SEQ directive is in effect by default.

     The .DOSSEG directive also causes the linker to generate two
     special symbols, _end and _edata. Do not use these names for your
     own symbols.
.context ASSUME
.context NOTHING
.context ERROR
.context @ASSUME
..index ASSUME directive
.topic Register Assumptions
.freeze 3
 \i\p\aExample\v@helloc.asm\v\i\p                                   \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   ASSUME segregister:seglocation [, segregister:seglocation]...

            ASSUME dataregister:qualifiedtype
            [, dataregister:qualifiedtype]...

            ASSUME register:ERROR [, register:ERROR]...

            ASSUME [register:]NOTHING [, register:NOTHING]...

  \bSee also:\p SEGMENT, GROUP, .MODEL, SEG, \aSimplified Segment Directives\v@SimpleSeg\v,
            \aComplete Segment Directives\v@CompSeg\v

  \bDescription:\p

     A general-purpose directive that allows the assembler to
     determine offsets, select the correct segment register, and verify
     certain register use assumptions at assembly.

     The "ASSUME segregister:seglocation" syntax is used to inform the
     assembler of the current value of <segregister>. The assembler
     uses this information to calculate offsets correctly and to guard
     against segment selection errors. This directive does not generate
     code to change the value of <segregister>. The ASSUME directive is
     not necessary for the CS register, since CS is assumed to the
     current segment or group automatically. The <seglocation> can be any
     segment or group name, or an equivalent expression.

     The "ASSUME dataregister:type" syntax informs the assembler that
     <dataregister> is subject to the constraints of <type>. If an
     assumed register is used incorrectly, the assembler generates an
     error. The <type> parameter must be the same size as
     <dataregister>.

       ASSUME   bx:WORD
       mov      ax, [bx]        ;Error--cannot dereference non-pointer
       ASSUME   bx:PTR WORD
       mov      [bx], 10h       ;Assumes operand size is WORD
       mov      [bx], al        ;Error--cannot move byte into word memory
       ASSUME   al:WORD         ;Error--al is only byte-sized
       ASSUME   bx:BYTE         ;Error--bx is word-sized

       The ASSUME directive can include ERROR, FLAT, or register:type.
       MASM 6.1 issues a warning when you specify ASSUME values for
       CS other than the current segment or group.

     The "ASSUME register:ERROR" syntax causes the assembler to
     generate an error if <register> is later used explictly or
     implicitly. The OPATTR, .TYPE, and TYPE directives do not cause
     errors when referring to registers assumed to ERROR.

     The "ASSUME register:NOTHING" syntax causes the assembler to
     remove all assumptions about <register>. ASSUME NOTHING removes
     all assumptions about all registers.
  
     Use an ASSUME statement each time you load a known value into a
     segment register. Although not required, the ASSUME directive
     helps the assembler safeguard against segment selection errors and
     determine offsets. You may need to use instructions to initialize
     segment registers properly at run time.

     If you use the ASSUME directive to set assumptions about a data
     register, it will override any assumptions for its subregisters.
     For example, ASSUME EDX:DGROUP will override assumptions for DL,
     DH, and DX.

     \bParameter\p      \bDescription\p

     segregister    CS, DS, ES, FS, GS, or SS. The assembler
                    automatically maintains assumptions for the CS
                    segment register.

     seglocation    A segment address, another segment register, a
                    group, or FLAT. You can find the segment address
                    of a label by using the SEG operator. FLAT is
                    the label of the single FLAT segment.

     dataregister   Any byte, word, or 32-bit register.

     qualifiedtype  Any qualified type that will fit in
                    <dataregister>.
                    See: \i\p\aQualified Type\v@QualifiedType\v\i\p

     register       Any segment or data register.
.context @.BREAK
.context BREAK
.context .BREAK
..index .BREAK directive
..index exiting loops
..index exiting .IF
..index .IF,, exiting
..index loops,, exiting
.topic Exit Iterative Directives
.freeze 3
 \i\p\aExample\v@loops.asm\v\i\p                                   \i\p\aUp\v@DirectCF\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .BREAK [.IF condition]

  \bSee also:\p .CONTINUE, .IF, .REPEAT, .WHILE, \aControl-Flow Directives\v@DirectCF\v

  \bDescription:\p

     Terminates the nearest enclosing .WHILE or .REPEAT directive. If
     the .IF condition is present, the loop is terminated only if the
     value of <condition> is true.
.context \@CatStr
.context @CatStr
.context CATSTR
..index concatenating strings, @CatStr Macro Function
..index strings,, concatenating, @CatStr Macro Function
..index @CatStr macro function
..index concatenating strings, CATSTR Directive
..index strings,, concatenating, CATSTR Directive
..index CATSTR directive
.topic Concatenate Strings
.freeze 3
 \i\p\aExample\v@SaveRegs.asx\v\i\p                                   \i\p\aUp\v@StringOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @CatStr(string[, string...])

            name CATSTR [textitem[, textitem...]]

  \bSee also:\p SUBSTR, SIZESTR, INSTR, TEXTEQU, \aAngle Brackets (<>)\v@anglebrackets\v,
            \aexpression operator (%)\v@percent\v

  \bDescription:\p

     The @CatStr macro function concatenates one or more text items.
     The result can be used anywhere a text item is appropriate. The
     @CatStr function will not expand macros or expressions unless
     you use the \aexpression operator (%)\v@percent\v.

     The CATSTR directive concatenates strings and assigns the result
     to <name>. These two lines are equivalent:

          language   TEXTEQU  @CatStr (<Quick>, <Pascal>)
          language   CATSTR   <Quick>, <Pascal>

     The <name> is a unique symbolic name, <string> is a text string,
     and <textitem> is a text item.
     See: \i\p\atext item\v@textitem\v\i\p
.context @.CODE
.context CODE
.context .CODE
..index .CODE directive
.freeze 3
.topic Start Code Segment
 \i\p\aExample\v@hello.asm\v\i\p                                   \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .CODE [name]

  \bSee also:\p @code, .MODEL, .DATA, .STACK, @CodeSize,
            \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Starts a code segment (with segment name <name>, if given)
     and ends the previous segment, if any. Aligns the segment on a
     2-byte boundary (.8086, .186, .286) or a 4-byte boundary (.386,
     .486). The .MODEL directive must precede this directive.

     Segment name <name> is an optional parameter that overrides the
     default segment name. If <name> is not specified, the assembler
     generates a segment called _TEXT (tiny, small, compact, and flat
     models) or <modulename>_TEXT (medium, large, and huge models).
.context @COMM
.context COMM
..index COMM directive
.freeze 3
.topic Create Communal Variables
                                             \i\p\aUp\v@ScopeVisibility\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   COMM [langtype] [NEAR³FAR] label:type[:count]
            [,[langtype] [[NEAR³FAR] label:type[:count]]...

  \bSee also:\p \aCOMM diagram\v@externcommdiag\v, EXTERNDEF, PUBLIC, .MODEL,
            \aTable of Memory Models\v@MemoryModels\v, \a/C<x> Command-Line Option\vml.hlp!/Cx\v,
            \aMASM 5.1 Compatibility\v@510commoff\v

  \bDescription:\p

     Creates one or more communal variables. Communal variables are
     uninitialized, public, and allocated by the linker. The assembler
     has no control over the order, position, or initial value of
     communal variables. Communal variables are usually declared in
     include files. Use EXTERNDEF to include data variables from other
     languages.

     \bParameter\p      \bDescription\p

     NEAR³FAR       Communal variable distance. PROC is interpreted
                    as NEAR or FAR depending on the memory model
                    chosen.

     langtype       Any language type. Determines how the linker
                    outputs variable names.
                    See: \i\p\alanguage type\v@langtype\v\i\p

     label          Name of the variable.

     type           Type specifier (BYTE, WORD, etc.), constant, or
                    structure name.

     count          Specifies the number of data objects in an array
                    (1 is the default).
.context @COMMENT
.context COMMENT
..index COMMENT directive
.freeze 3
.topic Multiline Comments
                                             \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   COMMENT delimiter [text]
              [text]
            [text] delimiter [text]

  \bSee also:\p \aSemicolon (;)\v@semicolon\v, \aSemicolons (;;)\v@semicolon\v

  \bDescription:\p

     Ignores all text between delimiters or on the same line as a
     delimiter. The two comment delimiters can be on the same or
     different physical lines.

     In the following code segment, the text inside the asterisks and
     the MOV instruction are ignored:

          COMMENT *  This text is ignored
          by the assembler *  mov    ax, 10h

     \bParameter\p      \bDescription\p

     delimiter      First nonblank character following the COMMENT
                    directive

     text           All text up to and including the line containing
                    the next occurrence of the delimiter.
.context @.CONST
.context CONST
.context .CONST
..index CONST directive
.freeze 3
.topic Start a Constant Data Block
                                             \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .CONST

  \bSee also:\p .MODEL, .CODE, .DATA, .FARDATA, .STACK, @data, @DataSize,
            \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Starts a constant data segment (called CONST in DGROUP) and ends
     the previous segment, if any. For consistency with other
     languages, this segment should contain all strings, real numbers,
     and other constant data accessed by a high-level language. The
     CONST segment has the READONLY attribute.

     The .MODEL directive must precede this directive.
.context @.CONTINUE
.context CONTINUE
.context .CONTINUE
.topic Evaluate Loop Condition
.freeze 3
..index .CONTINUE directive
 \i\p\aExample\v@loops.asm\v\i\p                                   \i\p\aUp\v@DirectCF\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .CONTINUE [.IF condition]

  \bSee also:\p .BREAK, .REPEAT, .UNTIL, .WHILE

  \bDescription:\p

     Skips to the code that evaluates the loop condition of the nearest
     enclosing loop. If the .IF condition clause is present, the skip
     will occur only if the value of <condition> is true.

     This directive is valid only within a .REPEAT or .WHILE block.
.context @.CREF
.context .CREF
.context CREF
.context .NOCREF
.context NOCREF
.context .XCREF
.context XCREF
.topic Turn On/Off Symbol Cross-Referencing
.freeze 3
..index .CREF directive
..index .NOCREF directive
                                             \i\p\aUp\vListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .CREF

            .NOCREF [name[, name]...]

  \bSee also:\p \a/Fl\vml.hlp!/fl\v, \a/FR\vml.hlp!/FR\v, \a/Sn\vml.hlp!/Sn\v

  \bDescription:\p

     The .CREF directive enables collection of information for all
     symbols. These symbols will appear in the symbol table at the end
     of a listing file. If you generate a source-browser file with the
     /FR or /Fr command-line option, the browser will be able to access
     cross-reference information for these symbols. This directive is
     in effect by default.

     The .NOCREF directive turns off cross-referencing for all symbols.
     Any references to symbols while .NOCREF is in effect are omitted
     from the cross-reference listing. If the <name> parameter is given
     with this directive, only the cross-reference information for the
     symbols specified by <name> is omitted.

     .XCREF is a synonym for .NOCREF and is provided for compatibility
     with previous versions of the assembler.
.context @.DATA
.context .DATA
.context .DATA?
.context DATA
..index .DATA directive
..index .DATA? directive,Start an Uninitialized Data Segment
.freeze 3
.topic Start a Data Segment
 \i\p\aExample\v@hello.asm\v\i\p                                   \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .DATA

            .DATA?

  \bSee also:\p .FARDATA, \aUninitialized Data Placeholder (?)\v@questionmark\v, .MODEL, .CODE,
            .CONST, .STACK, @data, @DataSize, \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     The .DATA directive starts the initialized data segment (with
     segment name _DATA) and ends the previous segment, if any. The
     .DATA segment should contain all global data that have initial
     values.

     The .DATA? directive starts the uninitialized data segment (with
     segment name _BSS) and ends the previous segment, if any. Place all
     uninitialized data in the .DATA? segment.

     Although uninitialized data can be placed in the .DATA segment by
     declaring data with the \a? operator\v@questionmark\v, use the .DATA? directive to
     minimize the size of the .EXE file and to maximize compatibility
     with other languages.

     The .MODEL directive must precede these directives. It generates a
     GROUP statement that places _DATA and _BSS in DGROUP.
.context DB
.context BYTE
.context SBYTE
.context @BYTE
.context DW
.context WORD
.context SWORD
.context DD
.context DWORD
.context SDWORD
.context DF
.context FWORD
.context DQ
.context QWORD
.context DT
.context TBYTE
.context REAL4
.context REAL8
.context REAL10
..index TBYTE directive
..index QWORD directive
..index FWORD directive, Declare Variables (Farword)
..index DWORD directive, Declare Variables (Doubleword)
..index SDWORD directive, Declare Variables (Signed Doubleword)
..index WORD directive
..index SWORD directive, Declare Variables (Signed Word)
..index BYTE directive, Declare Variables (Byte)
..index SBYTE directive, Declare Variables (Signed Byte)
..index REAL4 directive
..index REAL8 directive
..index REAL10 directive
.freeze 3
.topic Declare Variables
 \i\p\aExample\v@data.asm\v\i\p                                   \i\p\aUp\v@LangDirectives\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   [name] [S]BYTE initializer [, initializer]...

            [name] [S]WORD initializer [, initializer]...

            [name] [S]DWORD initializer [, initializer]...

            [name] FWORD initializer [, initializer]...

            [name] QWORD initializer [, initializer]...

            [name] TBYTE initializer [, initializer]...

            [name] REAL4 initializer [, initializer]...

            [name] REAL8 initializer [, initializer]...

            [name] REAL10 initializer [, initializer]...

  \bDescription:\p

                            Integer
    Type    Abbr  Size      Range        Types Allowed
  ÚÄÄÄÄÄÄÄÂÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ BYTE  ³ DB  ³ 1 byte  ³ -128 to    ³ Character, String                ³
  ³       ³     ³         ³ +255       ³                                  ³
  ÃÄÄÄÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ WORD  ³ DW  ³ 2 bytes ³ -32,768 to ³ 16-bit near ptr, 2 characters,   ³
  ³       ³     ³         ³ +65,535    ³ double-byte character            ³
  ÃÄÄÄÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ DWORD ³ DD  ³ 4 bytes ³ -2Gig to   ³ 16-bit far ptr, 32-bit near ptr, ³
  ³       ³     ³         ³ +4Gig-1    ³ 32-bit long word                 ³
  ÃÄÄÄÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ FWORD ³ DF  ³ 6 bytes ³    ÄÄ      ³ 32-bit far pointer               ³
  ÃÄÄÄÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ QWORD ³ DQ  ³ 8 bytes ³    ÄÄ      ³ 64-bit long word                 ³
  ÃÄÄÄÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ TBYTE ³ DT  ³10 bytes ³    ÄÄ      ³ BCD, 10-byte binary numbers      ³
  ÀÄÄÄÄÄÄÄÁÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
  ÚÄÄÄÄÄÄÄÂÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ REAL4 ³ DD  ³ 4 bytes ³    ÄÄ      ³ Single-precision floating point  ³
  ÃÄÄÄÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ REAL8 ³ DQ  ³ 8 bytes ³    ÄÄ      ³ Double-precision floating point  ³
  ÃÄÄÄÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ REAL10³ DT  ³10 bytes ³    ÄÄ      ³ 10-byte floating point           ³
  ÀÄÄÄÄÄÄÄÁÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

     These directives allocate and optionally initialize one or
     more bytes of global data. The S prefix indicates signed data,
     but this information is used internally by the assembler for
     comparisons used by \acontrol-flow directives\v@DirectCF\v and
     argument passing in INVOKE statements. CodeView also uses this
     information to specify the data format.

     The abbreviations (such as DB, DD) are limited forms of the
     data-declaration directives; they cannot be used as types. They
     are included for compatibility with previous versions of the
     assembler.

     String initializers, available with the BYTE directive, allocate
     a byte for each character of the string. The characters are stored
     with the leftmost character in the lowest memory location so that

          BYTE   "WASHINGTON"

     would be assembled to

          57 41 53 48 49 4E 47 54 4F 4E    "WASHINGTON"

     Character initializers, however, are stored with the leftmost
     character in the highest memory location. They are also limited
     to the current expression size so that under the default
     OPTION EXPR32

          DWORD        "WASH"

     would be assembled to

          48 53 41 57  "HSAW"

     and under OPTION EXPR16

          WORD         "WA"

     would be assembled to

          41 57        "AW"

     Use the REAL4, REAL8, and REAL10 data types for floating-point
     data. They inform CodeView that the data is in floating-point
     format and cause the assembler to perform more stringent type
     checking.

     \bParameter\p    \bDescription\p

     name         Symbol name assigned to the variable. If no name is
                  assigned, the memory space is allocated, but the
                  starting address of the variable has no symbolic
                  name.

     initializer  A type shown in the table above, constant integer
                  expression, \a?\v@questionmark\v, or DUP expression. Separate multiple
                  values with commas or use the DUP operator.
.context ECHO
.context @ECHO
.context %OUT
.topic Display Message during Assembly
.freeze 3
..index ECHO directive
                                             \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   ECHO message

  \bSee also:\p .ERR, \aExpression Operator (%)\v@percent\v, IF, \aOUT Instruction\v@OUT\v,
            \aMASM 5.1 Compatibility\v@510out\v

  \bDescription:\p

     Displays message during assembly. The message is usually output to
     the screen, but you can redirect it to a file or another device.
     This directive can help show which statements were assembled. %OUT
     is a synonym for ECHO and is included for compatibility.

     The <message> is any line of ASCII characters up to but not
     including any comment on the line. If angle brackets are used to
     enclose <message>, the angle brackets are displayed. To perform
     macro substitution, put the \amacro substitution operator (%)\v@percent\v at the
     beginning of the line. Use a separate ECHO directive for each line.

     To output the time and date of assembly, insert this line in your
     source code:

        % ECHO       Assembled on @Date at @Time
.context END
.context @END
..index END directive
.freeze 3
.topic End Source File
 \i\p\aExample\v@hellomo.asm\v\i\p                                   \i\p\aUp\v@DirectCF\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   END [address]

  \bSee also:\p .STARTUP, .EXIT, ORG

  \bDescription:\p

     Marks the end of a source file and optionally indicates the
     program load address. The optional <address> is a label or
     expression identifying where program execution begins. You can
     define <address> only once in a program, usually in the main
     module.

     You cannot specify <address> if you have used the .STARTUP
     directive, which automatically sets a start address. If you are
     linking with a high-level language, the start address is
     typically set by that language's compiler.

     END also closes the last segment in the source file.
.context ENDM
.context @ENDM
..index ENDM directive
.freeze 3
.topic End Macro and Iterative Blocks
 \i\p\aExample\v@LoadPtr.asx\v\i\p                                   \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   ENDM

  \bSee also:\p MACRO, REPEAT, FOR, FORC, WHILE, EXITM,
            \aMacro and Iterative Blocks\v@MacroIter\v

  \bDescription:\p

     Terminates macro and iterative blocks.
.context ENDS
.context @ENDS
..index ENDS directive
.freeze 3
.topic End Segment and Structure Blocks
 \i\p\aExample\v@struct.asm\v\i\p                                   \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name ENDS

  \bSee also:\p SEGMENT, STRUCT, UNION, \a.DATA\v@.DATA\v, \a.DATA?\v@.DATA\v, @CurSeg, .STACK

  \bDescription:\p

     Marks the end of a segment or a structure definition previously
     started with SEGMENT, STRUCT, or UNION. To end a simplified segment
     (such as one started with .DATA), use the line

          @CurSeg   ENDS          ;End current segment

     A single .STACK statement opens and closes the stack segment,
     so you should not include an additional ENDS statement.
.context TEXTEQU
.context EQU
.context @EQU
..index equate,, text, TEXTEQU
..index EQU directive, String Equate with Macro Substitution
..index TEXTEQU directive, String Equate with Macro Substitution
.freeze 3
.topic Numeric and String Equate
 \i\p\aExample\v@argi.asx\v\i\p                                   \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name EQU expression

            name EQU <text>

            name TEXTEQU textmacro

            name TEXTEQU %expression

  \bSee also:\p \aEqual Sign (=)\v@equalsign\v, EQ, \a==\v@ccompare\v, \aAngle Brackets (<>)\v@anglebrackets\v,
            \aEscape character (!)\v@exclamation\v, \atext item\v@textitem\v, \a/D\vml.hlp!/D\v, \aMASM 5.1 Compatibility\v@510equtype\v

  \bDescription:\p

     The EQU directive assigns <expression> or <text> to <name>. Angle
     brackets indicate that <text> should not be evaluated as an
     expression.

     EQU evaluates the numeric value of <expression> and assigns
     the resulting constant value to <name>. Unlike =, EQU does not
     allow numeric equates to be redefined. If <expression> cannot be
     evaluated, EQU treats <expression> as text.

     The TEXTEQU directive acts like the EQU directive with text
     equates but performs macro substitution at assembly and does not
     require angle brackets. The TEXTEQU directive will also resolve
     the value of an expression preceded by a percent sign (%). The EQU
     directive does not perform macro substitution or expression
     evaluation for strings.

     Text equates are limited to 255 characters. Text equates can be
     redefined to another value but not to a different type of symbol.

     \bParameter\p      \bDescription\p

     name           Symbolic name for <expression> or <text>

     expression     Variable or constant numeric expression (not a
                    memory expression)

     text           String enclosed in angle brackets (<>)

     textmacro      A text macro (such as @Date) or a string enclosed in
                    angle brackets (<>).

     expression     An expression preceded by a %.

  \bExample:\p

       MASM    EQU      5.1 + 0.9        ;Evaluates to 6.0
       Msft    EQU      <Microsoft>      ;String equate
       libpath TEXTEQU  @Environ(<LIB>)  ;Text Macro Equate
       endstr  TEXTEQU  %(endadr-staradr);String equate to numeric value

     In MASM 5.1, you can equate one symbol with another. These equates are
     called aliases. Unless you specify OPTION M510, MASM 6.1 does not allow
     aliases defined with EQU. An immediate expression or text must appear as
     the right operand of an EQU directive. Change aliases to use the TEXTEQU
     directive. This change may cause an expression to evaluate differently.
.context @.ERR
.context .ERR
.context ERR
..index ERR directive
.freeze 3
.topic Generate Severe Error
 \i\p\aExample\v@LoadPtr.asx\v\i\p                                   \i\p\aUp\v@CondErr\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .ERR [message]

  \bSee also:\p .ERRE, .ERRB, .ERRDEF, .ERRDIF, \aAngle Brackets(<>)\v@anglebrackets\v

  \bDescription:\p

     Generates an \aA2052 error\vml.hlp!A2052\v message when assembled. Generally, this
     directive occurs in a conditional-assembly block and is used to
     indicate some error condition discovered at assembly time.
     This directive can be useful for verifying that the program
     assembled correctly and that user-defined bounds were not exceeded.

     If <message> is given, it will be displayed with the standard
     assembler error messages. The <message> parameter is any text item
     See: \i\p\atext item\v@textitem\v\i\p
.context @ERR1
.context ERR1
.context ERR2
.context .ERR1
.context .ERR2
.freeze 3
.topic Generate Pass-Dependent Error
                                             \i\p\aUp\v@CondErr\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .ERR1 [message]

  \bSee also:\p IF1, .ERR, .ERRE, .ERRB, .ERRDEF, .ERRDIF

  \bDescription:\p

     The .ERR1 directive is identical to the .ERR directive.  Since MASM
     6.1 is a single-pass assembler, this directive is supplied for
     compatibility with previous versions of the assembler.

     The .ERR2 directive is supported only when when MASM 5.10
     compatibility is enabled with the /Zm option.

     If <message> is given, it will be displayed with the standard
     assembler error messages. The <message> is any text item.
     See: \i\p\atextitem\v@textitem\v\i\p
.context @.ERRB
.context .ERRB
.context ERRB
.context .ERRNB
.context ERRNB
..index .ERR[N]B directive
.freeze 3
.topic Generate Error for Missing Argument
                                             \i\p\aUp\v@CondErr\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .ERRB <textitem> [,message]

            .ERRNB <textitem> [,message]

  \bSee also:\p IFB, IFNB, .ERR, .ERRE, .ERRDEF, .ERRDIF, MACRO, PROC

  \bDescription:\p

     The .ERRB directive generates an \aA2057 error\vml.hlp!A2057\v if <textitem> is
     blank. The .ERRNB directive generates an \aA2058 error\vml.hlp!A2058\v if <textitem>
     is not blank.

     These directives are usually used within macro definitions; a
     macro parameter is replaced by an empty string if you call the
     macro and do not give a corresponding argument. These directives
     let you write macros that generate an assembly-time error if the
     wrong number of arguments is used in the macro call. However, the
     :REQ parameter attribute or the variable argument feature often
     provides the same functionality in a more straightforward way.

     \bParameter\p      \bDescription\p

     textitem       Item to be tested, any text item.
                    See: \i\p\atextitem\v@textitem\v\i\p

     message        Programmer-supplied error message, any text item.
                    See: \i\p\atextitem\v@textitem\v\i\p
.context @.ERRDEF
.context .ERRDEF
.context ERRDEF
.context ERRNDEF
.context .ERRNDEF
..index ERR[N]DEF directive
.freeze 3
.topic Generate Error for Undefined Symbol
                                             \i\p\aUp\v@CondErr\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .ERRDEF name [,message]

            .ERRNDEF name [,message]

  \bSee also:\p IFDEF, IFNDEF, .ERR, .ERRE, .ERRB, .ERRDIF

  \bDescription:\p

     The .ERRDEF directive generates an \aA2056 error\vml.hlp!A2056\v message if <name>
     is a previously defined symbol. The .ERRNDEF directive generates
     an \aA2055 error\vml.hlp!A2055\v message if <name> has not been previously
     defined.

     One of the most common uses of the .ERRNDEF directive is to ensure
     that a symbol has been defined before you attempt to use it. This
     situation can occur if the source code depends on the command-line
     \a/D option\vml.hlp!/d\v or the \aMacro Assembler Global Options dialog box\vml.hlp!d.6300\v to
     define a symbol.

     If <message> is given, it will be displayed with the standard
     assembler error messages. The <message> parameter is any text item.
     See: \i\p\atextitem\v@textitem\v\i\p
.context .ERRDIF
.context ERRDIF
.context ERRIDN
.context .ERRIDN
.context @.ERRIDN
.context ERRDIFI
.context .ERRDIFI
.context ERRIDNI
.context .ERRIDNI
..index .ERRDIF/.ERRIDN directive, Generate Error If Arguments Same/Different
.freeze 3
.topic Generate Error If Arguments Are Same/Different
                                             \i\p\aUp\v@CondErr\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .ERRDIF[I] <textitem1>, <textitem2> [,message]

            .ERRIDN[I] <textitem1>, <textitem2> [,message]

  \bSee also:\p IFDIF, IFIDN, .ERR, .ERRE, .ERRB, .ERRDEF

  \bDescription:\p

     The .ERRDIF directive generates an \aA2060 error\vml.hlp!A2060\v if <textitem1> and
     <textitem2> are different. .ERRIDN generates an \aA2059 error\vml.hlp!A2059\v if the
     arguments are identical. These directives are commonly used inside
     macros to test for bad values passed as parameters. The .ERRDIFI
     and .ERRIDNI directives act the same as the .ERRDIF and .ERRIDN
     directives but perform case-insensitive comparisons.

     The <textitem1> and <textitem2> parameters are text items.
     See: \i\p\atextitem\v@textitem\v\i\p

     If <message> is given, it will be displayed with the standard
     assembler error messages. The <message> parameter is any text item.
     See: \i\p\atextitem\v@textitem\v\i\p
.context @.ERRE
.context ERRE
.context .ERRE
.context ERRNZ
.context .ERRNZ
..index .ERRE/.ERRNZ directive, Generate Error If Argument Zero/Nonzero
.freeze 3
.topic Generate Error If Argument Is Zero/Nonzero
                                             \i\p\aUp\v@CondErr\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .ERRE expression [,message]

            .ERRNZ expression [,message]

  \bSee also:\p IF, IFE, .ERR, .ERRB, .ERRDEF, .ERRDIF

  \bDescription:\p

     Tests value of <expression> and generates an error depending on
     the resulting value. The .ERRE directive generates an \aA2053 error\vml.hlp!A2053\v
     if <expression> evaluates to false (zero). .ERRNZ generates an
     \aA2054\vml.hlp!A2054\v \aerror\vml.hlp!A2052\v if <expression> evaluates to true (nonzero). If
     <message> is given, it will be displayed with the standard
     assembler error message.

     \bParameter\p      \bDescription\p

     expression     Expression that evaluates to true, false, or a
                    numeric constant. The expression cannot contain
                    forward references.

     message        Programmer-supplied error message, any text item.
                    See: \i\p\atextitem\v@textitem\v\i\p
.context EXITM
.context @EXITM
..index EXITM directive
.freeze 3
.topic End Macro and Repeat Blocks
 \i\p\aExample\v@argi.asx\v\i\p                                   \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   EXITM [textitem]

  \bSee also:\p FOR, FORC, REPEAT, MACRO, ENDM

  \bDescription:\p

     Terminates expansion of the current macro or repeat block and
     begins assembly of the next statement outside the block. If the
     EXITM directive is encountered in a nested macro block, the
     assembler returns to expanding the previous block level.

     The <textitem> parameter is a text item that is returned to the
     calling statement. The <textitem> field is valid only with an
     EXITM directive at the end of a MACRO block.
     See: \i\p\atextitem\v@textitem\v\i\p
.context ABS
.context EXTRN
.context EXTERN
.context @EXTERNDEF
.context EXTERNDEF
..index ABS
..index EXTERN directive, Define External Variables/Labels/Symbols
..index EXTERNDEF directive, Define External Variables/Labels/Symbols
.freeze 3
.topic Define External Variables, Labels, and Symbols
 \i\p\aExample\v@global.asx\v\i\p                                   \i\p\aUp\v@ScopeVisibility\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   EXTERNDEF [langtype] name:qualifiedtype
            [, [langtype] name:qualifiedtype]...

            EXTERN [langtype] name[(altID)]:qualifiedtype
            [, [langtype] name[(altID)]:qualifiedtype]...

  \bSee also:\p \aEXTERNDEF Diagram\v@externcommdiag\v, PUBLIC, COMM, PROTO, \a/C<x>\vml.hlp!/Cx\v, \a/H\vml.hlp!/H\v, \a/Zf\vml.hlp!/Zf\v,
            \aMASM 5.1 Compatibility\v@510extern\v

  \bDescription:\p

     The EXTERNDEF directive tells the assembler that one or more
     variables, symbols, data structures, or labels are defined in
     other modules. The behavior of the EXTERNDEF directive is
     determined by its context:

        ş If <name> is used in the current module but is not defined,
          <name> is made external.
        ş If <name> is defined in the current module, it is made
          PUBLIC.
        ş If <name> is neither used nor defined, the directive is
          ignored.

     The EXTERNDEF directive allows you to put global data declarations
     in an include file without forcing the linker to pull in unneeded
     modules. Usually, you define <name> in one module and put the
     EXTERNDEF directive in a file that is included in all program
     modules. The PROTO directive automatically generates an EXTERNDEF
     for its related procedure.

     EXTERN is a less flexible version of EXTERNDEF. It declares
     <name> as external whether or not it is used in the module.
     EXTRN is a synonym for EXTERN and is included for compatibility
     with previous versions of the assembler.

     With the EXTERN directive, <name> can be followed by (altID),
     which allows the linker to select an alternate resolution for
     <name>. This is useful when creating library files.

     \bParameter\p     \bDescription\p

     langtype      A language type appropriate to <name>.
                   See: \i\p\alanguage type\v@langtype\v\i\p

     name          A unique global symbolic name. Case mapping is
                   overridden by \aOPTION CASEMAP\v@CASEMAP\v.

     qualifiedtype The qualified type to be given to <name>. Can
                   also be ABS, which causes <name> to be imported
                   as a constant. Does not have to match type given
                   to <name> in original definition.
                   See: \i\p\aqualified type\v@qualifiedtype\v\i\p

     altID         A unique global symbolic name to use as an alternate
                   for <name>. Must be of the same type as <name>. The
                   <altID> parameter cannot be declared as a <name>
                   with an altID in an EXTERN statement in this or any
                   other module.

     In MASM 5.1, you must place an EXTRN directive for a variable in the
     same segment that holds the variable. For far data, this often entails
     opening and closing a segment just to place the EXTRN statement.

     In MASM 6.1, this same-segment requirement does not apply with the
     EXTERN and EXTERNDEF statements. The only requirement is that
     they aren't located in the wrong segment. In compatibility mode,
     however, MASM 6.1 emulates the behavior of MASM 5.1.
.context @externcommdiag
.freeze 3
.topic EXTERNDEF/COMM Diagram
                                             \i\p\aUp\v@EXTERNDEF\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     Variables can be shared between source files in the following
     manner:

                      ÚÄTOOLS.INCÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
                      ³                          ³
                      ³ EXTERNDEF  Global: WORD  ³ÄDeclaration
                      ³ COMM       Communal: WORD³ÄDeclaration &
                      ÀÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÙ  definition
                                   ³
                  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
                  ³                                  ³
     ÚÄUSER.ASMÄÄÄÁÄÄÄÄÄÄÄÄÄÄ¿          ÚÄTOOLS.ASMÄÄÁÄÄÄÄÄÄÄÄÄÄ¿
     ³                       ³          ³                       ³
     ³ INCLUDE   TOOLS.INC   ³          ³ INCLUDE   TOOLS.INC   ³
     ³                       ³          ³                       ³
     ³ mov       Global, ax  ³ÂÄUse    ³ Global    WORD ?      ³ÄDefinition
     ³ mov       bx, Communal³Ù        ³                       ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ          ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

     The EXTERNDEF directive in TOOLS.INC declares the variable Global
     and allows it to be accessed within modules that include
     TOOLS.INC, such as USER.ASM. The EXTERNDEF directive does not
     define Global; it is created by TOOLS.ASM. If TOOLS.INC is
     included by a module that does not use Global, the linker will not
     import Global.

     The COMM directive in TOOLS.INC declares and defines the variable
     Communal and exports it to all modules that include TOOLS.INC,
     regardless of whether or not Communal is used.
.context @.FARDATA
.context .FARDATA
.context .FARDATA?
.context FARDATA
.context FARDATA?
..index FARDATA? directive, Start a FAR Undefined Segment
..index FARDATA directive
.freeze 3
.topic Start a Far Data Segment
                                             \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .FARDATA [name]

            .FARDATA? [name]

  \bSee also:\p .DATA, .MODEL, .CODE, .CONST, .STACK, SEG, @data, @fardata,
            @DataSize, \aTable of Memory Models\v@MemoryModels\v, \aMASM 5.1 Compatibility\v@510questionmark\v

  \bDescription:\p

     The .FARDATA directive starts a far data segment for initialized
     data (segment name FAR_DATA or <name>, if given), ending any
     previous segment. The .FARDATA? directive starts a far data
     segment for uninitialized data (segment name FAR_BSS or
     <segmentname>, if given), ending any previous segment.

     Data in segments defined with .FARDATA and .FARDATA? are not
     placed in a group or combined with far data segments for other
     source modules. Using <name> allows you to create multiple far
     data segments in one source module. The .MODEL directive must
     precede these directives.

     Putting uninitialized arrays and buffers into a segment created
     by .FARDATA? rather than .FARDATA can decrease the size of object
     files and libraries.

     Normally, you cannot access far data segments by name, because
     there may be multiple \aPRIVATE segments\v@segment\v with the same name. Use
     SEG expressions instead.

  \bExample:\p

   ÚÄMODULE1.ASMÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
   ³                                                                       ³
   ³         .FARDATA?                                                     ³
   ³ array   WORD      1000 DUP ?                                          ³
   ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

   ÚÄMODULE2.ASMÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
   ³                                                                       ³
   ³         mov       ax, SEG array   ;Right                              ³
   ³         mov       ax, @fardata    ;Wrong, as different modules        ³
   ³                                   ;create different far data segments ³
   ³         mov       ds, ax                                              ³
   ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
.context @FOR
.context FOR
.context IRP
.topic Iterative Macro
.freeze 3
..index FOR directive, Iterative Assembly
..index FOR directive, Iterative Assembly (for Character)
 \i\p\aExample\v@argi.asx\v\i\p                                   \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   FOR parameter[:REQ³:=default], <argument [,argument]...>
              statements
            ENDM

  \bSee also:\p ENDM, EXITM, FORC, REPEAT, WHILE, \aMASM 5.1 Compatibility\v@510ampers\v

  \bDescription:\p

     Repeats <statements> once for each argument specified. The current
     argument replaces <parameter> in each iteration. IRP is a synonym
     for FOR and is included for compatibility with previous versions
     of the assembler.

     \bParameter\p      \bDescription\p

     parameter      A valid symbol name. If parameter appears in
                    statements, it is replaced by a different argument
                    in each iteration.

     :REQ           Optional tag for <parameter> causing an assembler
                    error if <parameter> is given a blank argument.

     :=default      Optional tag for <parameter> causing <parameter> to
                    be assigned <default> if given a blank argument.

     argument       Text, symbol, string, or numeric constant. Separate
                    multiple iterations with commas. Angle brackets are
                    required if a string <argument> includes spaces or
                    commas. Leading spaces of string arguments are
                    ignored.

     statements     Any valid assembler statements.
.context @FORC
.context FORC
.context IRPC
.topic Iterative Macro (for Character)
.freeze 3
..index FORC directive
                                             \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   FORC parameter, <string>
              statements
            ENDM

  \bSee also:\p ENDM, EXITM, FOR, REPEAT, WHILE, \aString Delimiters ("/')\v@quotes\v,
            \aMASM 5.1 Compatibility\v@510ampers\v

  \bDescription:\p

     Repeats <statements> once for each character in the string. The
     character replaces <parameter> in each repetition. IRPC is a synonym
     for FORC and is included for compatibility with previous versions
     of the assembler.

     \bParameter\p      \bDescription\p

     parameter      A valid symbol name. If <parameter> appears in
                    statements, then it is replaced by a different
                    character each iteration.

     string         A literal string or a symbol defined with a string
                    equate. All spaces are counted as characters. Angle
                    brackets are required.

     statements     Any valid assembler statements.
.context GOTO
.context @GOTO
..index GOTO directive
.freeze 3
.topic Skip to Label
                                             \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   GOTO macrolabel

  \bSee also:\p \aMacro Labels (:/::)\v@colon\v, .BREAK, .CONTINUE, MACRO

  \bDescription:\p

     Causes assembler to skip to <macrolabel>. The GOTO directive is
     only allowed inside a MACRO, FOR, FORC, REPEAT, or WHILE block.
     The <macrolabel> is only visible within the block.

  \bExample:\p

     MACRO     Instruments
               IFNDEF    Cymbal
                         GOTO     Crash
               ENDIF
               ù
               ù
               ù
               :Crash
                         .ERR     <Undefined symbol.>
               ù
               ù
               ù
     ENDM
.context GROUP
.context @GROUP
..index GROUP directive
..index segments,, combining, GROUP Directive
..index combining segments, GROUP Directive
.freeze 3
.topic Combine Segments
 \i\p\aExample\v@hellomo.asm\v\i\p                                   \i\p\aUp\v@CompSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name GROUP segment [,segment]...

  \bSee also:\p ASSUME, SEGMENT

  \bDescription:\p

     Adds the <segment> parameters to a group called <name>. Creates
     group <name> if it does not exist. This causes the linker to
     combine the given logical segments into one physical segment, so
     that the logical segments share a common physical segment address.
     Under the FLAT memory model, all segments are automatically
     assigned to a common group called FLAT.
     See: \i\p\aFLAT model\v@Flat\v\i\p

     All logical segments in a group must have the same WordSize
     attribute. If they are 16-bit segments, their total size cannot
     exceed 64K bytes. If they are 32-bit segments (allowed on
     80386/486 processors), groups can be up to 4 gigabytes.

     The <name> is a unique symbolic name or previously defined group.
     If the same group name appears in more than one GROUP statement,
     the effect is cumulative; the new segments are added to the group.
     The <segment> is a segment name defined with the SEGMENT directive.
.context @if_block
.context ENDIF
.context ELSE
..index IF block syntax
..index ENDIF directive
..index ELSE directive
.topic IF Block Syntax
.freeze 3
 \i\p\aExample\v@LoadPtr.asx\v\i\p                                   \i\p\aUp\v@CondAssem\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   IFcond expression
              ifstatements
            [ELSEIFcond expression
              elseifstatements]
              ù
              ù
              ù
            [ELSE
              elsestatements]
            ENDIF

  \bSee also:\p \aConditional Assembly Directives\v@CondAssem\v, IF, \a.IF\v@.IF\v, \a.ELSE\v@.if\v, \a.ENDIF\v@.if\v,
            \aControl-Flow Directives\v@DirectCF\v

  \bDescription:\p

     The IF block format can be used to create a source file that will
     generate a variety of programs, depending on constants or
     environment variables. For example, you can create a single source
     file that generates code for different memory models and operating
     environments. IF blocks can be nested up to 20 levels but cannot
     span include files or structure, union, or macro definitions.

     The statements following the ELSE directive are assembled only if
     the preceding IF directive was found to be false (zero). Only one
     ELSE statement is allowed in each IF block, although there can be
     several ELSEIF statements.

     IFcond is one of the conditional assembly directives beginning
     with "IF" (such as IF, IFE, and IFB). There is an optional
     ELSEIFcond corresponding to each IFcond.

     The <ifstatements>, <elseifstatements>, and <elsestatements> are
     each a series of one or more assembly-code statements. The
     respective conditions determine which block is assembled, if any.
     The ELSE and ELSEIF blocks are optional.
.context .if
.context @.if
.context .else
.context .elseif
.context .endif
..index .IF directive
..index .ELSE directive
..index .ELSEIF directive
..index .ENDIF directive
.freeze 3
.topic Control-Flow Blocks
 \i\p\aExample\v@ifs.asm\v\i\p                                   \i\p\aUp\v@DirectCF\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .IF condition
               ifstatements
             [.ELSEIF condition
               elseifstatements]
               .
               .
               .
             [.ELSE
               elsestatements]
             .ENDIF

  \bSee also:\p IF, \aConditional Assembly Directives\v@CondAssem\v, \aComparison Operators\v@CCompare\v,
            \aControl-Flow Directives\v@DirectCF\v

  \bDescription:\p

     Used for clear coding of common control-flow blocks. Generates
     comparison and conditional jump instructions so that if
     <condition> is true, the processor executes the instructions
     following .IF until the next .ELSEIF, .ELSE, or .ENDIF. .IF
     statements can be nested.

     The .ELSEIF statement is used to create a code block that is
     executed if the preceding .IF and .ELSEIF conditions are false,
     and the current .ELSEIF condition is true. There can be several
     .ELSEIF blocks within a .IF block. The .ELSEIF statement performs
     the same function as a .IF inside a .ELSE but does not create
     another nesting level of .IFs.

     .ELSE specifies an alternate code block if the matching .IF and
     previous .ELSEIF expressions are false.

     .ENDIF closes the current .IF, .ELSEIF, or .ELSE code block.
     The assembler optimizes to get the best possible code. For example,

         .IF  ax==0
     performs the comparison with:
         or   ax,ax
     which is more efficient than:
         cmp  ax, 0
.context @IF
.context IF
.context IFE
.context ELSEIF
.context ELSEIFE
..index IF[E] directive, Assemble if Expression True or False
..index ELSEIF[E] directive, Assemble if Expression True or False
.freeze 3
.topic IF/IFE/ELSEIF/ELSEIFE Directives
 \i\p\aExample\v@LoadPtr.asx\v\i\p                                   \i\p\aUp\v@CondAssem\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   [ELSE]IF expression

            [ELSE]IFE expression

  \bSee also:\p \a.IF\v@.if\v, \aConditional Assembly Directives\v@CondAssem\v, \aRelational Operators\v@RelationalOp\v,
            .ERRE, .ERRNZ

  \bDescription:\p

     IF causes assembly of a block of statements if expression is true
     (nonzero). IFE causes assembly of a block of statements if
     expression is false (zero).
     See: \i\p\aIF Block Syntax\v@if_block\v\i\p

     The <expression> must resolve to a constant and contain no forward
     references.
.context IF1
.context IF2
.context ELSEIF1
.context ELSEIF2
.freeze 3
.topic IF1/IF2/ELSEIF1/ELSEIF2 Directives
                                             \i\p\aUp\v@CondAssem\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   [ELSE]IF1

  \bSee also:\p \aConditional Assembly Directives\v@CondAssem\v, \aIF Block Syntax\v@if_block\v, .ERR1

  \bDescription:\p

     Unconditionally assembles the code in the IF block. Since the
     assembler is now single-pass, the IF1 and ELSEIF1 directives are
     always considered true. Supplied for compatibility with previous
     versions of the assembler.

     The [ELSE]IF1 and [ELSE]IF2 directives are no longer supported.
.context IFB
.context @IFB
.context IFNB
.context ELSEIFB
.context ELSEIFNB
..index ELSEIF[N]B directive, Assemble if Argument Blank/Not Blank
..index IF[N]B directive, Assemble if Argument Blank/Not Blank
.freeze 3
.topic IFB/IFNB/ELSEIFB/ELSEIFNB Directives
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   [ELSE]IFB expression

            [ELSE]IFNB expression

  \bSee also:\p \aConditional Assembly Directives\v@CondAssem\v, \aRelational Operators\v@RelationalOp\v,
            .ERRB, .ERRNB, MACRO

  \bDescription:\p

     IFB causes assembly of a block of statements if <expression> is
     blank. IFNB causes assembly of a block of statements if <expression>
     is not blank. The <expression> field is any name, number, or
     expression; typically, it is the name of a parameter of a macro.
     See: \i\p\aIF Block Syntax\v@if_block\v\i\p

     Within a macro, a parameter name is replaced by an empty string if
     you call the macro without giving a corresponding argument. IFB
     and IFNB let you define default behavior when an argument is
     missing, but the :REQ and :=default MACRO parameters are preferred.
.context IFDEF
.context @IFDEF
.context IFNDEF
.context ELSEIFDEF
.context ELSEIFNDEF
..index ELSEIF[N]DEF directive, Assemble if Symbol Defined/Not Defined
..index IF[N]DEF directive, Assemble if Symbol Defined/Not Defined
.freeze 3
.topic IFDEF/IFNDEF/ELSEIFDEF/ELSEIFNDEF Directives
                                              \i\p\aUp\v@CondAssem\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   [ELSE]IFDEF name

            [ELSE]IFNDEF name

  \bSee also:\p \aConditional Assembly Directives\v@CondAssem\v, \aRelational Operators\v@RelationalOp\v,
            .ERRDEF, .ERRNDEF

  \bDescription:\p

     IFDEF causes assembly of a block of statements if <name> is a
     previously defined symbol. IFNDEF causes assembly of a block of
     statements if name has not been defined.
     See: \i\p\aIF Block Syntax\v@if_block\v\i\p

     You can define symbols on the command line or in the \aMacro Assembler\vml.hlp!d.6300\v
     \aGlobal Options dialog box\vml.hlp!d.6300\v.
     See: \i\p\a/Dsymbol\vml.hlp!/d\v\i\p

     Use of this feature with the IFDEF and IFNDEF directives lets you
     control which statements are assembled from the command line.
.context IFDIF
.context @IFIDN
.context IFIDN
.context IFDIFI
.context IFIDNI
.context ELSEIFDIF
.context ELSEIFIDN
.context ELSEIFDIFI
.context ELSEIFIDNI
..index IFDIF[I] directive, Assemble if Arguments Different
..index IFIDN[I] directive, Assemble if Arguments Identical
..index ELSEIFDIF[I] directive, Assemble if Arguments Different
..index ELSEIFIDN[I] directive, Assemble if Arguments Identical
.freeze 3
.topic IFDIF/ELSEIFDIF Variations
 \i\p\aExample\v@LoadPtr.asx\v\i\p                                   \i\p\aUp\v@CondAssem\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   [ELSE]IFDIF[I] textitem1, textitem2

            [ELSE]IFIDN[I] textitem1, textitem2

  \bSee also:\p \aConditional Assembly Directives\v@CondAssem\v, \aRelational Operators\v@RelationalOp\v,
            .ERRDIF, .ERRIDN

  \bDescription:\p

     Causes assembly of a block of statements depending on whether two
     textitems are different or identical. IFDIF assembles the block if
     <textitem1> and <textitem2> are different. IFIDN assembles the block
     if they are the same. IFDIFI and IFIDNI do the same actions but are
     not case sensitive.
     See: \i\p\aIF Block Syntax\v@if_block\v\i\p

     The <textitem1> and <textitem2> parameters are text items.
     See: \i\p\atextitem\v@textitem\v\i\p
.context INCLUDE
.context @INCLUDE
.topic Insert Source Code
.freeze 3
..index INCLUDE directive
                                        \i\p\aUp\v@ScopeVisibility\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
.comment the Example was os2
.comment \i\p\aExample\v@hellos2.asm\v\i\p                               \i\p\aUp\v@ScopeVisibility\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   INCLUDE filename

            INCLUDE <filename>

  \bSee also:\p INCLUDELIB, \aAngle brackets (<>)\v@anglebrackets\v, \a/I\vml.hlp!/I\v, \a/X\vml.hlp!/X\v

  \bDescription:\p

     Inserts source code from <filename> into the current source file
     during assembly. If <filename> does not include a fully-specified
     path, the assembler searches the directory of the current file,
     the \a/I command-line\vml.hlp!/I\v include path or the PWB include path, and then
     the INCLUDE environment variable. Nested INCLUDE directives are
     allowed.

     The <filename> parameter is an existing file containing valid
     assembly-language statements and directives. No default file
     extension is assumed. Angle brackets are required if the filename
     or path includes a backslash, semicolon, greater-than, less-than,
     single quotation marks, or double quotation marks.
.context INCLUDELIB
.context @INCLUDELIB
.topic Link with LIB File
.freeze 3
..index specifying library files
..index INCLUDELIB directive, Include with Library (LIB) File
                                        \i\p\aUp\v@ScopeVisibility\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
.comment the Example was os2
.comment \i\p\aExample\v@hellos2.asm\v\i\p                               \i\p\aUp\v@ScopeVisibility\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   INCLUDELIB libraryname

            INCLUDELIB <libraryname>

  \bSee also:\p INCLUDE, \aAngle Brackets (<>)\v@anglebrackets\v, \a/link\vml.hlp!/link\v

  \bDescription:\p

     Informs the linker that the current module should be linked with
     the <libraryname>. If <path> is not given, the linker searches the
     current working directory, directories given in the ML or LINK
     library field, and then the LIB environment variable from the
     TOOLS.INI file.
.comment
.comment     A common use of INCLUDELIB is to specify OS2.LIB, which is
.comment     required for all system calls under OS/2.

     The <libraryname> parameter is an existing file containing code in
     LIB format. If no extension is specified, the default extension
     .LIB is assumed. Angle brackets are required if the libraryname
     or path includes a backslash, semicolon, greater-than, less-than,
     single quotation marks, or double quotation marks.
.context \@InStr
.context @InStr
.context INSTR
.topic Search for String
..index INSTR directive
..index searching strings, INSTR Directive
..index strings,, searching, INSTR Directive
..index @InStr macro function
.freeze 3
 \i\p\aExample\v@RestoreR.asx\v\i\p                                   \i\p\aUp\v@StringOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @InStr([position], string1, string2)

            name INSTR [position,] textitem1, textitem2

  \bSee also:\p SUBSTR, SIZESTR, CATSTR, TEXTEQU, \aAngle Brackets (<>)\v@anglebrackets\v,
            \aexpression operator (%)\v@percent\v

  \bDescription:\p

     The @InStr macro function searches for the first occurrence of
     <string2> inside <string1> and returns the position of the
     first character of the match. The search begins at <position>,
     if you give that parameter. The @InStr function will not expand
     macros or expressions unless you use the \aexpression operator (%)\v@percent\v.

     The first character is indexed as 1. @InStr returns 0 if
     <string1> is not found.

     The INSTR directive performs the same function as @InStr but
     assigns the result to <name>. These two lines are equivalent:

          develop    TEXTEQU    @InStr (2, <MikeBobMichaelJim>, <Mi>)
          develop    INSTR      2, <MikeBobMichaelJim>, <Mi>
                                ;develop is now 8

     \bParameter\p      \bDescription\p

     name           Name of the resulting constant.

     position       Starting character position of the search
                    (default 1). Must be a positive integer.

     string1        String being searched; any text.

     string2        String to search for; any text.

     textitem1      String being searched; any text item.
                    See: \i\p\atext item\v@textitem\v\i\p

     textitem2      String to search for; any text item.
                    See: \i\p\atext item\v@textitem\v\i\p
.context INVOKE
.context ADDR
.context @INVOKE
.topic Call Stack-Based Procedures
.freeze 3
..index INVOKE directive
 \i\p\aExample\v@hellom.asx\v\i\p                                   \i\p\aUp\v@Labeling\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   INVOKE expression [,arguments]

  \bSee also:\p \aProcedure Diagram\v@proccommdiag\v, PROTO, .MODEL, \aOPTION LANGUAGE:\v@LANG\v

  \bDescription:\p

     Automates the call interface to stack-based procedures. Handles all
     pushing of parameters onto the stack. Cleans up the stack when the
     procedure returns.

     The <invokelist> is passed to the procedure according to the types
     given in that procedure's prototype. If necessary, the assembler
     will generate code to convert the elements of <invokelist> to the
     types specified in the prototype.

     \bParameter\p      \bDescription\p

     expression     Expression yielding address or label of procedure
                    to be called. Must resolve to a valid address and
                    cannot include any forward references.

     arguments      Sequential list of parameters passed. Can be
                    register::register (for a register pair), expression,
                    or ADDR label. ADDR label passes the address of
                    <label> (segment and offset if DWORD, segment only
                    if WORD) to the procedure.
.context LABEL
.context @LABEL
.topic Create Label
.freeze 3
..index LABEL directive
 \i\p\aExample\v@data.asm\v\i\p                                   \i\p\aUp\v@Labeling\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name LABEL qualifiedtype

  \bSee also:\p STRUCT, UNION, PROC, THIS, \aLabel (:/::)\v@colon\v, \aLocal label (@@:)\v@at_F\v

  \bDescription:\p

     Creates a new label of a given type at a specified location by
     assigning the current location-counter value and the given type to
     <name>. The <qualifiedtype> parameter determines the type of data
     or call distance for the label. This directive does not allocate
     data.

     \bParameter\p      \bDescription\p

     name           Symbol assigned to the label.

     qualifiedtype  Any qualified type.
                    See: \i\p\aqualified type\v@qualifiedtype\v\i\p
.context @.LIST
.context .LIST
.context LIST
.topic List Source Statements to .LST File
.freeze 3
..index .LIST directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .LIST

  \bSee also:\p .LISTALL, .NOLIST, .LISTIF, .NOLISTIF, .TFCOND,
            .LISTMACROALL, .LISTMACRO, .NOLISTMACRO, PUSHCONTEXT, \a/Fl\vml.hlp!/Fl\v,
            \a/S<x>\vml.hlp!/Sx\v

  \bDescription:\p

     Enables listing of all source statements to the .LST file. The
     .LST file must first be enabled with a PWB MASM option or the
     \a/Fl command-line option\vml.hlp!/Fl\v. This directive is in effect by default.
.context @.LISTALL
.context .LISTALL
.context LISTALL
.topic List All Statements to .LST File
.freeze 3
..index .LISTALL directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .LISTALL

  \bSee also:\p .LIST, .NOLIST, .LISTIF, .NOLISTIF, .TFCOND,
            .LISTMACROALL, .LISTMACRO, .NOLISTMACRO, \a/Fl\vml.hlp!/Fl\v, \a/S<x>\vml.hlp!/Sx\v

  \bDescription:\p

     Enables listing of all statements to the .LST file. The .LST file
     must first be enabled with a PWB MASM option or the \a/Fl command-line\vml.hlp!/Fl\v
     \aoption\vml.hlp!/Fl\v. This directive is equivalent to the combination of the
     .LIST, .LISTIF, and .LISTMACROALL directives.
.context .LFCOND
.context LFCOND
.context .LISTIF
.context @.LISTIF
.context LISTIF
.topic List Statements in Conditional Blocks
.freeze 3
..index .LISTIF directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .LISTIF

  \bSee also:\p .LISTALL, .LIST, .NOLIST, .NOLISTIF, .TFCOND,
            .LISTMACROALL, .LISTMACRO, .NOLISTMACRO, \a/Fl\vml.hlp!/Fl\v, \a/S<x>\vml.hlp!/Sx\v

  \bDescription:\p

     Enables listing of all statements in conditional blocks, even
     those in false blocks. This directive is in effect by default. The
     .LFCOND directive is a synonym of .LISTIF and is included for
     compatibility with previous versions of the assembler.
.context @.LISTMACRO
.context .LISTMACRO
.context XALL
.context .XALL
.context LISTMACRO
.topic Enable Assembly Listing File
.freeze 3
..index .LISTMACRO directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .LISTMACRO

  \bSee also:\p .LISTALL, .LIST,  .NOLIST, .LISTIF, .NOLISTIF, .TFCOND,
            .LISTMACROALL, .NOLISTMACRO, \a/Fl\vml.hlp!/Fl\v, \a/S<x>\vml.hlp!/Sx\v

  \bDescription:\p

     Enables an assembly listing file that includes macro expansion
     statements that generate code or data. Comments, equates, and
     segment definitions are not listed. This directive is in effect by
     default. The .LFCOND directive is a synonym of .LISTMACRO and is
     included for compatibility with previous versions of the
     assembler.
.context @.LISTMACROALL
.context .LISTMACROALL
.context LISTMACROALL
.context .LALL
.context LALL
.topic List Source Statements in Macro Expansion
.freeze 3
..index .LISTMACROALL directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .LISTMACROALL

  \bSee also:\p .LISTALL, .LIST, .NOLIST, .LISTIF, .NOLISTIF, .TFCOND,
            .LISTMACRO, .NOLISTMACRO, \a/Fl\vml.hlp!/Fl\v, \a/S<x>\vml.hlp!/Sx\v

  \bDescription:\p

     Enables listing of all source statements in a macro expansion,
     including normal comments but excluding macro comments. The .LALL
     directive is a synonym of .LISTMACROALL and is included for
     compatibility with previous versions of the assembler.
.context @LOCAL.proc
..index LOCAL directive
.freeze 3
.topic Local Variables in Procedures
 \i\p\aExample\v@pause.asx\v\i\p                                   \i\p\aUp\v@Labeling\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   LOCAL name [[count]]\a[:qualifiedtype]\v@qualifiedtype\v [, name [[count]]
            \a[:qualifiedtype]\v@qualifiedtype\v]...

  \bSee also:\p \aLOCAL in Macros\v@LOCAL.mac\v, PROC

  \bDescription:\p

     Generates code to create one or more stack (automatic) variables,
     which can be accessed only within the current procedure. The
     assembler uses the same method used by high-level languages to
     create local variables.

     The <name> parameter is the name of the variable, and <count> is
     an optional expression (which must appear in square brackets)
     indicating the number of elements to allocate. The <qualifiedtype>
     parameter is any qualified type appropriate to <name>. The default
     <qualifiedtype> is WORD in a 16-bit segment and DWORD in a 32-bit
     segment.
     See: \i\p\aqualified type\v@qualifiedtype\v\i\p

     Once declared in a LOCAL statement, local variables can be
     referred to by name. The assembler translates references to these
     variables into references to their actual location on the stack
     using the BP indirect addressing mode.

     The assembler will generate an error if you have already defined
     <name> as a label.

  \bExample:\p

     LOCAL     array[20]:BYTE
.context @LOCAL.mac
..index LOCAL directive
.freeze 3
.topic Local Variables in Macros
 \i\p\aExample\v@argi.asx\v\i\p                                   \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   LOCAL localname [,localname]...

  \bSee also:\p \aLOCAL in Procedures\v@LOCAL.proc\v, MACRO, REPEAT, FOR, FORC, WHILE,

  \bDescription:\p

     Declares a label or symbol for local use within a macro or
     iterative block. The assembler generates a different internal name
     for the symbol each time it expands the macro. The LOCAL directive
     allows you to call a macro that contains local jump statements and
     variable definitions without generating the same label each time
     the macro is expanded.

     The <localname> parameter is the name of a label or symbol inside
     the macro. Inside the macro expansion, <localname> takes precedence
     over a global label or symbol with the same name.
.context MACRO
.context :REQ
.context REQ
.context :=
.context @MACRO
.topic Macro Block
.freeze 3
..index MACRO directive
 \i\p\aExample\v@LoadPtr.asx\v\i\p                                   \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name MACRO [parameter[:tag]] [,parameter[:tag]]...]
              [\aLOCAL\v@local.MAC\v varlist]
              statements
              [EXITM [textitem]]
            ENDM

  \bSee also:\p ENDM, \aLOCAL\v@local.MAC\v, EXITM, \aOPTION OLDMACROS\v@OLDMACROS\v, .LISTMACRO,
            FARDATA, PUSHCONTEXT, \aUsing VARARG in Macros\v@VARARG.mac\v,
            \aMASM 5.1 Compatibility\v@510ampers\v

  \bDescription:\p

     Marks the beginning of a macro definition called <name> and takes
     optional parameters. The assembler generates the statements in the
     macro block each time the macro is called in source code. Macro
     procedures and macro functions can be nested up to 40 levels. Text
     macros may be nested up to 20 levels.

     Macro procedures can be redefined at any time. The new definition
     affects any subsequent calls to this macro. Macro functions can
     only be redefined, if they have not been called yet.

  \bExample:\p

     mymacro MACRO     value:REQ, reg:=<AX>, options:VARARG
             LOCAL     returnval
             ù
             ù
             ù
               EXITM      returnval
             ENDM

     \bParameter\p      \bDescription\p

     name           A unique symbol name. It can appear later in source
                    code, to call the macro.

     parameter      A valid symbol name. Each parameter can appear in
                    the statements and is replaced by the corresponding
                    item in the argument list whenever the macro is
                    called.

     :tag           Either :REQ, :=default, or :VARARG. :REQ causes an
                    assembler error if the parameter is given a blank
                    argument. :=default causes <parameter> to be
                    assigned <default> if given a null argument.
                    :VARARG allows a variable number of arguments to be
                    passed as a comma-separated list to <parameter>. If
                    :VARARG is used, it must be applied only to the
                    last parameter of the MACRO directive.

     statements     Any valid assembler statements.

     value          Optional return value for macro function.
.context .MODEL
.context OS_DOS
.context OS_OS2
.context NEARSTACK
.context FARSTACK
.context @.MODEL
.context MODEL
.topic Initialize Memory Model
.freeze 3
..index .MODEL directive
 \i\p\aExample\v@hello.asm\v\i\p                                   \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .MODEL memorymodel [,langtype] [,ostype] [,stackoption]

  \bSee also:\p .CODE, .DATA, .STACK, ASSUME, GROUP, EXTERN, PUBLIC,
            PROC, PROTO, @CodeSize, @DataSize, @Model, @Interface,
            \a.MODEL with .386 and .486\v@mem386\v, \aMASM 5.1 Compatibility\v@510modoffset\v

  \bDescription:\p

     Initializes the program memory model. Must be placed at the
     beginning of the source file before any other simplified segment
     directive. Generates ASSUME and GROUP statements used by the
     specified memory model. Also creates the predefined equates for
     the various simplified segments. The .MODEL directive can occur
     only once in a program.

     If you use the langtype argument with .MODEL, nested procedures
     are not allowed.

     \bParameter\p      \bDescription\p

     memorymodel    TINY, SMALL, COMPACT, MEDIUM, LARGE, HUGE, or
                    FLAT. If you are writing an assembler routine for
                    a high-level language, this option should match
                    the memory model used by the compiler or
                    interpreter. FLAT is valid only in .386 or .486
                    mode. You must use the /AT command-line option or
                    /TINY linker option to assemble tiny-model programs.
                    See: \i\p\aMemory Models\v@MemoryModels\v\i\p
                         \i\p\a/AT command-line option\vml.hlp!/AT\v\i\p

     langtype       Any language type. Instructs the assembler to
                    follow the naming, calling, and return conventions
                    for the indicated language. This affects the
                    PUBLIC, EXTERN, PROC, PROTO, and INVOKE
                    directives.
                    See: \i\p\alanguage type\v@langtype\v\i\p

    ostype         OS_DOS is the only option currently supported
                    and is the default.
                    See: \i\p\a.STARTUP\v@.STARTUP\v\i\p
.comment     ostype         OS_DOS or OS_OS2. Determines the behavior of the
.comment                    .STARTUP and .EXIT commands for compatibility with
.comment                    these operating systems. DOS is the default.
.comment                    See: \i\p\a.STARTUP\v@.STARTUP\v\i\p

     stackoption    NEARSTACK (if SS equals DS) or FARSTACK (if SS is
                    not equal to DS). NEARSTACK is the default. The
                    stack is not part of DGROUP with the FARSTACK
                    option. This affects the code generated by the
                    .STARTUP directive.
.context .MSFLOAT
.context MSFLOAT
.freeze 3
.topic .MSFLOAT directive
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     The .MSFLOAT directive and the Microsoft Binary Format are no
     longer supported.
.context NAME
.freeze 3
.topic NAME
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   NAME modulename

  \bSee also:\p TITLE, SUBTITLE, PAGE

  \bDescription:\p

     The NAME directive is ignored by the assembler. It is provided for
     compatibility with previous versions of the assembler.
.context .NOLIST
.context @.NOLIST
.context NOLIST
.context .XLIST
.context XLIST
.topic Suppress Program Listing
.freeze 3
..index .NOLIST directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .NOLIST

  \bSee also:\p .LISTALL, .LIST, .LISTIF, .NOLISTIF, .TFCOND,
            .LISTMACROALL, .LISTMACRO, .NOLISTMACRO

  \bDescription:\p

     Suppresses program listing. Use in conjunction with .LIST to
     prevent a particular section of a source file from being listed.
     The .XLIST directive is a synonym for .NOLIST and is included for
     compatibility with previous versions of the assembler.
.context @.NOLISTIF
.context .NOLISTIF
.context .SFCOND
.context SFCOND
.context NOLISTIF
.topic Turn off List
.freeze 3
..index .NOLISTIF
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .NOLISTIF

  \bSee also:\p .LISTALL, .LIST, .NOLIST, .LISTIF, .TFCOND,
            .LISTMACROALL, .LISTMACRO, .NOLISTMACRO

  \bDescription:\p

     Turns off the listing of the lines in subsequent conditional blocks
     whose condition is false. The .SFCOND directive is a synonym for
     .NOLISTIF and is included for compatibility with previous versions
     of the assembler.
.context .NOLISTMACRO
.context @.NOLISTMACRO
.context NOLISTMACRO
.context .SALL
.context SALL
.topic Suppress Macro Listing
.freeze 3
..index .NOLISTMACRO directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .NOLISTMACRO

  \bSee also:\p .LISTALL, .LIST,  .NOLIST, .LISTIF, .NOLISTIF, .TFCOND,
            .LISTMACROALL, .LISTMACRO

     Suppresses listing of macro expansions in a listing file. Macro
     calls are listed, but the source code generated by the call is
     not. The .SALL directive is a synonym for .NOLISTMACRO and is
     included for compatibility with previous versions of the
     assembler.
.context OPTION
.context @OPTION
..index OPTION directive
.freeze 3
.topic Set Assembler Options
                                             \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION optionlist

  \bDescription:\p

    Modifies the behavior of certain features of the assembler. The
    <optionlist> is a list of one or more of the following options.
    Separate options with commas. These options affect the module from
    the point they are used onward and override corresponding command-line
    options:

     \bOption\p                        \bDefault\p

     \i\p\aCASEMAP:maptype\v@CASEMAP\v\i\p             NOTPUBLIC
     \i\p\aDOTNAME³NODOTNAME\v@DOTNAME\v\i\p           NODOTNAME
     \i\p\aEMULATOR³NOEMULATOR\v@float\v\i\p         NOEMULATOR
     \i\p\aEPILOGUE:macroid\v@prologue\v\i\p            Depends on OS and processor type
     \i\p\aEXPR16³EXPR32\v@expr1632\v\i\p               Depends on processor
     \i\p\aLANGUAGE:langtype\v@lang\v\i\p           -none-
     \i\p\aLJMP³NOLJMP\v@LJMP\v\i\p                 LJMP
     \i\p\aM510³NOM510\v@M510\v\i\p                 NOM510
     \i\p\aNOKEYWORD:<keywordlist>\v@nokeyword\v\i\p     -none-
     \i\p\aNOSIGNEXTEND\v@nosignextend\v\i\p                Off
     \i\p\aOFFSET:offsettype\v@offset:\v\i\p           GROUP
     \i\p\aOLDMACROS³NOOLDMACROS\v@OLDMACROS\v\i\p       NOOLDMACROS
     \i\p\aOLDSTRUCTS³NOOLDSTRUCTS\v@OLDSTRUCTS\v\i\p     NOOLDSTRUCTS
     \i\p\aPROC:visibility\v@optproc\v\i\p             PUBLIC
     \i\p\aPROLOGUE:macrofuncid\v@prologue\v\i\p        Depends on OS and processor type
     \i\p\aREADONLY³NOREADONLY\v@READONLY\v\i\p         NOREADONLY
     \i\p\aSCOPED³NOSCOPED\v@SCOPED\v\i\p             SCOPED
     \i\p\aSEGMENT:segmentsize\v@optseg\v\i\p         USE16
     \i\p\aSETIF2:{TRUE³FALSE}\v@optsetif2\v\i\p         TRUE
.context SETIF2
.context @SETIF2
.context @optsetif2
..index MASM 5.1 two-pass compiler
..index two-pass compiler,, MASM 5.1-Compatible
.freeze 3
.topic SETIF2:{TRUE³FALSE} Option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   SETIF2:{TRUE³FALSE}

  \bSee also:\p \aMASM 5.1 Compatibility\v@510ampers\v

  \bDescription:\p

     By default, MASM 6.1 does not recognize pass-dependent constructs.
     Unlike version 5.1, MASM 6.1 does most of its work on its first pass,
     then performs as many subsequent passes as necessary. In contrast,
     MASM 5.1 always assembles in two source passes.

     Both the OPTION M510 and OPTION SETIF2 statements force MASM 6.1 to
     handle MASM 5.1 constructs that activate on the second assembly pass,
     such as .ERR2, IF2, and ELSEIF2.

     To assure compatibility, MASM 6.1 supports 5.1 directives referring to
     two passes. These include .ERR1, .ERR2, IF1, IF2, ELSEIF1, and ELSEIF2.
     For second pass constructs, you must specify OPTION SETIF2. Without
     OPTION SETIF2, the IF2 and .ERR2 directives cause error A2061

     When set to TRUE, OPTION SETIF2 forces all second-pass constructs
     to activate on every assembly pass. When set to FALSE, second-pass
     constructs do not activate on any pass. OPTION M510 implies
     OPTION SETIF2:TRUE.

     MASM 6.1 handles first-pass constructs differently. It treats the .ERR1
     directive as .ERR, and the IF1 directive as IF.
.context @M510
.context M510
.context NOM510
.topic Maximize Compatibility with MASM 5.1
.freeze 3
..index MASM 5.1 compatibility, M510³NOM510 option
..index M510 option
..index NOM510 option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION M510

            OPTION NOM510

  \bSee also:\p \a/Zm\vml.hlp!/Zm\v

  \bDescription:\p

     The M510 option sets all options to maximize compatibility with
     MASM 5.10. NOM510 is the default. OPTION M510 enables the
     following:

     \i\p\aOLDMACROS\v@OLDMACROS\v\i\p
     \i\p\aOLDSTRUCTS\v@OLDSTRUCTS\v\i\p
     \i\p\aDOTNAME\v@DOTNAME\v\i\p
     \i\p\aCASEMAP:ALL\v@CASEMAP\v\i\p
     \i\p\aEXPR16\v@EXPR1632\v\i\p
     \i\p\aSETIF2:TRUE\v@SETIF2\v\i\p

     The M510 option enables \aOPTION SCOPED\v@SCOPED\v if a language type is specified
     in a .MODEL statement. If the .MODEL directive does not specify
     a language type, \aOPTION NOSCOPED\v@scoped\v is used.

     Using OPTION M510 also changes the default for \aOFFSET\v@offset:\v to \aGROUP\v@offset:\v if a
     .MODEL directive is in effect and to \aSEGMENT\v@offset\v otherwise.

     With OPTION M510, keywords and instructions that are not available
     in the current CPU mode (such as ENTER under .8086) are not treated as
     keywords. The USE32, FLAT, FAR32, and NEAR32 segment types and the
     80386/486 registers are not keywords with processor selection
     directives less than .386.

     In MASM 5.1, a large constant value that can fit only in the processor's
     default word (four bytes for .386 and .486, two bytes otherwise) is
     assigned a size attribute of the default word size. The value of the
     constant affects the number of bytes changed by the instruction.

     Without OPTION M510, the assembler never assigns a size automatically.
     You must state it explicitly with the PTR operator, as shown in the
     following example:

          ; Without OPTION M510
                  mov     [bx], WORD PTR 0100h

     MASM 5.1 allows a code label definition in a data definition statement
     if that statement does not also define a data label. MASM 6.1 also
     allows such definitions if OPTION M510 is enabled; otherwise it is
     illegal:

          ; Legal only with OPTION M510
                 MyCodeLabel:    DW      0

     By default, MASM 6.1 changes the way that expressions are evaluated.
     In MASM 5.1,

          var-2[bx]

     is parsed as

          (var-2)[bx]

     Without OPTION M510, you must rewrite the statement, since the assembler
     parses it as

          var-(2[bx])

     which generates an error.

     In compatibility mode, use the PTR operator to type-cast a constant to a
     structure type. This is most often done in data initializers to affect
     the CodeView information of the data label. Without OPTION M510, the
     assembler generates an error.

     MYSTRC  STRUC
            f1    DB      0
          MYSTRC  ENDS
          MyPtr   DW      MYSTRC PTR 0    ; Illegal without OPTION M510

     MASM 6.1 recursively expands text macros used as values, whereas
     MASM 5.1 simply replaces the text macro with its value. The following
     example illustrates the difference:

; With OPTION M510
                  tm1     EQU     <contains tm2>
                  tm2     EQU     <value>
                  tm3     CATSTR  tm1             ; == <contains tm2>

          ; Without OPTION M510
                  tm3     CATSTR  tm1             ; == <contains value>
.context @OLDMACROS
.context OLDMACROS
.context NOOLDMACROS
..index MASM 5.1-compatible macros
..index macros,, MASM 5.1-Compatible
.freeze 3
.topic OLDMACROS³NOOLDMACROS Option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION OLDMACROS

            OPTION NOOLDMACROS

  \bSee also:\p MACRO, \aAmpersand(&)\v@ampersand\v, \aMASM 5.1 Compatibility\v@510ampers\v

  \bDescription:\p

     The OLDMACROS option enables MASM 5.1-compatible macros and
     parameter substitution. NOOLDMACROS is the default. The OLDMACROS
     option is enabled with the M510 option.

     Without OPTION OLDMACROS or OPTION M510, MASM 6.1 changes the behavior of
     macros in several ways. If you want the MASM 5.1 macro behavior, add
     OPTION OLDMACROS or OPTION M510 to your MASM 5.1 code.

     MASM 5.1 allows white spaces or commas to separate arguments to macros.
     MASM 6.1 with OPTION NOOLDMACROS (the default) requires commas between
     arguments. For example, in the macro call

            MyMacro  var1 var2 var3, var4

     OPTION OLDMACROS causes the assembler to treat all four items as separate
     arguments. With OPTION NOOLDMACROS, the assembler treats

            var1 var2 var3

     as one argument since the items are not separated with commas. To
     convert your macro code, replace spaces between macro arguments with
     a single comma.

     The default OPTION NOOLDMACROS causes the assembler to interpret
     ampersands (&) within a macro differently than does MASM 5.1. MASM 5.1
     requires one ampersand for each level of macro nesting. OPTION OLDMACROS
     emulates this behavior.

     Without OPTION OLDMACROS, MASM 6.1 removes ampersands only once no 
     matter how deeply nested the macro. To update your MASM 5.1 macros,
     follow this simple rule: replace every sequence of ampersands with a
     single ampersand. The only exception is when macro parameters immediately
     precede and follow the ampersand, and both require substitution. In this
     case, use two ampersands.
.context @OLDSTRUCTS
.context OLDSTRUCTS
.context NOOLDSTRUCTS
..index MASM 5.1-compatible structure elements
..index structure elements, MASM 5.1-Compatible
.freeze 3
.topic OLDSTRUCTS³NOOLDSTRUCTS Option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION OLDSTRUCTS

            OPTION NOOLDSTRUCTS

  \bSee also:\p STRUCT, UNION, \aDot Operator (.)\v@period\v, \aMASM 5.1 Compatibility\v@510struct\v

  \bDescription:\p

     The OLDSTRUCTS option enables MASM 5.1-compatible scoping of
     structure members. This option causes all structure members to
     have the same scope as the structure name and prevents a label and
     a member name from having the same name. This option also causes
     the dot operator to act exactly like a plus.

     NOOLDSTRUCTS is the default. The OLDSTRUCTS option is enabled with
     the M510 option.With the default, OPTION NOOLDSTRUCTS, label and struc-
     ture field names may have the same name. With OPTION OLDSTRUCTS (the
     MASM 5.1 default), labels and structure fields cannot have the same name.

     With OPTION OLDSTRUCTS or OPTION M510 you can use plus operator (+) in
     structure field references. Labels and structure field names cannot have1
     the same name with OPTION OLDSTRUCTS.

     By default, each reference to structure member names must use the dot
     operator (.) to separate the structure variable name from the field name.
     You cannot use the dot operator as the plus operator (+) or vice versa.
     To convert your code so that it does not need OPTION OLDSTRUCTS, qualify
     all structure field references. and change all uses of the dot operator
     occurring outside of structure references to use the plus operator.

     If you remove OPTION OLDSTRUCTS from your code, the assembler generates
     errors for all lines requiring change. Using the dot operator in any
     context other than for a structure field results in error A2166. Unqual-
     ified structure references result in error A2006.
.context @LJMP
.context LJMP
.context NOLJMP
.topic Automatic Conditional Jump Lengthening
.freeze 3
..index LJMP option
..index jump lengthening,, automatic, LJMP Option
..index lengthening,, automatic, LJMP Option
..index conditional length jumping, LJMP Option
..index automatic conditional jump, LJMP Option
..index NOLJMP option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION LJMP

            OPTION NOLJMP

  \bSee also:\p \aConditional Jumps\v@Jcond\v

  \bDescription:\p

     The LJMP option enables automatic conditional-jump lengthening.
     LJMP is the default.

     Enabling LJMP allows the assembler to generate code that emulates
     a conditional jump of greater than -128 to +127 bytes. If the jump
     is within this range, no special code is generated. It does not
     affect unconditional jumps or the \acontrol-flow directives\v@DirectCF\v.

     The assembler will generate an A6003 warning for a lengthened jump
     so that you can identify it for later optimization.
.context @DOTNAME
.context DOTNAME
.context NODOTNAME
.topic Enable/Disable Dot Labels
.freeze 3
..index NODOTNAME option
..index DOTNAME option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION DOTNAME

            OPTION NODOTNAME

  \bSee also:\p \aDot (.)\v@period\v

  \bDescription:\p

     The DOTNAME option enables the use of the period (.) as the leading
     character in variable, label, macro, struct, union, and member names.
     NODOTNAME is the default. The DOTNAME option is enabled with the M510
     option, since previous versions of the assembler recognized names
     with period prefixes.

     The DOTNAME option does not have any effect on any of the reserved
     words. The use of the period in names is discouraged, as it can be
     confused with the field offset and structure operators.

     If you don't want to use this directive in your source code, rename the
     identifiers whose names begin with a period.
.context READONLY
.context @READONLY
.context NOREADONLY
.topic Make Code Segment Read-Only
.freeze 3
..index READONLY³NOREADONLY option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION READONLY

            OPTION NOREADONLY

  \bSee also:\p SEGMENT, .CODE

  \bDescription:\p

     The READONLY option sets the READONLY attribute for subsequent code
     segments. Enables checking for instructions that explicitly modify
     segments or groups containing code. Useful for detecting self-
     modifying code. NOREADONLY is the default.

     This option replaces the command-line /P option from previous
     versions of the assembler. Segments can be also be made read-
     only with the READONLY parameter of the SEGMENT directive.
.context @SCOPED
.context SCOPED
.context NOSCOPED
.topic Localize Label
.freeze 3
..index SCOPED option, Choose Label Scope
..index NOSCOPED option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION SCOPED

            OPTION NOSCOPED

  \bSee also:\p PROC, LABEL, \aColon (:/::)\v@colon\v

  \bDescription:\p

     The SCOPED option causes a label (defined with the "label:" syntax)
     to be local to the procedure in which it is defined. Labels defined
     with the LABEL directive or the "label::" syntax are not affected by
     this option.

     Using NOSCOPED will cause all labels to be global to the module.
     SCOPED is the default. The NOSCOPED option is enabled with the
     M510 option, if the .MODEL directive is used without a language type
     specification. Specifying .MODEL with a language type enables
     OPTION SCOPED.

     Without a language type, MASM 5.1 assumes code labels in procedures have
     no scope--that is, the labels are not local to the procedure. When not
     in compatibility mode, MASM 6.1 always gives scope to code labels, even
     without a language type. 

     To force MASM 5.1 behavior, specify either OPTION M510 or OPTION NOSCOPED
     in your code. To selectively enable MASM 6.1 behavior, place the dir-
     ective OPTION SCOPED after OPTION M510.

     To determine which labels require change, assemble the module without the
     OPTION NOSCOPED directive. For each reference to a label that is not
     local, the assembler generates error A2006.
.context @LANG
.context LANGUAGE
.topic Specify Default Language Type
.freeze 3
..index LANGUAGE option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION LANGUAGE:langtype

  \bSee also:\p PROC, PROTO, INVOKE, EXTERN, EXTERNDEF, PUBLIC, .MODEL,
            \a/G<x>\vml.hlp!/Gc\v

  \bDescription:\p

     Specifies the default language type to be used with many
     directives. The most recent OPTION LANGUAGE: or .MODEL directive
     determines the current language type. The <langtype> parameter is
     any language type.
     See: \i\p\aLanguage Type\v@langtype\v\i\p
.context ALL
.context NONE
.context NOTPUBLIC
.context @CASEMAP
.context CASEMAP
.topic Control Case Mapping 
.freeze 3
..index CASEMAP option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION CASEMAP:maptype

  \bSee also:\p \a/C<x>\vml.hlp!/Cx\v

  \bDescription:\p

     Controls the mapping of characters to uppercase. The <maptype>
     parameter can be ALL, NONE, or NOTPUBLIC, which correspond to the
     \a/Cu\vml.hlp!/cu\v, \a/Cp\vml.hlp!/cp\v, and \a/Cx\vml.hlp!/Cx\v command-line options. You can use this option
     only once per source file. The current language type, if any, will
     override <maptype>.
     See: \i\p\aLanguage Type\v@langtype\v\i\p

     CASEMAP:NOTPUBLIC is the default. The CASEMAP option is set to ALL
     with the M510 option.
.context @FLOAT
.context EMULATOR
.context NOEMULATOR
.topic Floating-Point Code Generation
.freeze 3
..index EMULATOR option
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION EMULATOR
            OPTION NOEMULATOR

  \bSee also:\p \a/FPi\vml.hlp!/Fpi\v

  \bDescription:\p

     Controls how floating-point instructions are generated. NOEMULATOR,
     the default, generates the coprocessor instructions directly.
     EMULATOR generates instructions as well as special fixup records
     for the linker so that the floating-point emulator can be used. This
     option is definable only once per source file.

     To use this option, you must link your assembly-language module with
     an emulator library from a Microsoft high-level language.

     OPTION EMULATOR is equivalent to the \a/FPi command-line option\vml.hlp!/FPi\v.
.context @OFFSET:
.context OFFSET:
..index OFFSET option
.freeze 3
.topic Offset Relative to Segment/Group
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION OFFSET:offsettype

  \bSee also:\p \aMASM 5.1 Compatibility\v@510offoff\v, OFFSET, GROUP, SEGMENT,
            \aFLAT memory model\v@flat\v

  \bDescription:\p

     Controls whether the OFFSET operator returns offsets relative to
     the current segment or group. OPTION OFFSET:GROUP causes OFFSET
     to return a group-relative offset if the label is in a segment
     that is part of a group. OPTION OFFSET:SEGMENT causes OFFSET
     to return a segment-relative offset. OPTION OFFSET:GROUP is the
     default.

     The FLAT option causes fixups to be relative to the FLAT group.

     OPTION M510 sets OPTION OFFSET:GROUP if a .MODEL directive has been
     used and OPTION OFFSET:SEGMENT otherwise. With previous versions of
     the assembler, the OFFSET type was SEGMENT with \acomplete segment\v@CompSeg\v
     \adirectives\v@CompSeg\v and GROUP with \asimplified segment directives\v@simpleseg\v.

     The <offsettype> can be GROUP, SEGMENT, or FLAT.

     When programming for the 80386 or 80486, the size of an OFFSET expression
     can be 2 bytes for a symbol in a USE16 segment, or 4 bytes for a symbol
     in a USE32 or FLAT segment. With OPTION M510, you can use a 32-bit
     OFFSET expression in a 16-bit context. Without OPTION M510, you must
     use the LOWWORD operator to convert the offset size.

     With no .MODEL, MASM 5.1 computes offsets from the start of the segment,
     whereas MASM 6.1 computes offsets from the start of the group. (With
     .MODEL, MASM 5.1 also computes offsets from the start of the group.)

     To force MASM 6.1 to emulate 5.1 behavior, specify either OFFSET:SEGMENT
     or OPTION M510. Both directives cause the assembler to compute offsets
     relative to the segment if you do not include .MODEL.

     To selectively enable MASM 6.1 behavior, place the directive OPTION
     OFFSET:GROUP after OPTION M510. In this case, you should ensure each
     OFFSET statement has a segment override where appropriate.

     When not in compatibility mode, the OPTION OFFSET directive determines
     whether the SEG operator returns a value relative to the group or
     segment. With OPTION M510, SEG is always segment-relative by default,
     regardless of the current value of OPTION OFFSET.
.context @PROLOGUE
.context PROLOGUE
.context EPILOGUE
.topic Register Macro as Prologue or Epilogue
.freeze 3
..index PROLOGUE option,Register Macro as PROLOGUE
..index EPILOGUE option,Register Macro as EPILOGUE
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION PROLOGUE:macroname

            OPTION EPILOGUE:macroname

  \bSee also:\p PROC, MACRO, \aRET Instruction\v@RETN\v, \aIRET Instruction\v@IRET\v,
            \aUsing VARARG in Macros\v@vararg.mac\v

  \bDescription:\p

     PROLOGUE registers <macroname> as the macro function to be called
     when a prologue needs to be generated. EPILOGUE registers
     <macroname> as the macro procedure to be called when a RET or IRET
     instruction is encountered.

     You can revert to the default prologue or epilogue by specifying
     PrologueDef or EpilogueDef as <macroname>. EPILOGUE code is
     generated only if the RET or IRET instruction terminating the PROC
     block has no operand. The RETN, RETF, and IRETF instructions do not
     cause the assembler to generate epilogue code.

     To suppress generation of the prologue or epilogue code, give NONE
     as the <macroname>.

     The assembler expects a macro definition of this form:

     macroname MACRO procname, flags, argbytes, localbytes, <reglist>,
                     userparms:VARARG

     \bParameter\p      \bDescription\p

     procname       Macro name for the procedure or NONE.

     flags          Word-size bit flag: (1=On, 0=Off)
                    \bBits    Description\p
                    0-2     Language type:
                              000     Not specified
                              001     C
                              010     SYSCALL
                              011     STDCALL
                              100     Pascal
                              101     Fortran
                              110     Basic
                              111     Reserved
                    3       Reserved
                    4       Caller restores stack
                    5       FAR procedure
                    \a6       PRIVATE procedure\v@proc\v
                    7       EXPORT procedure
                    8       Epilogue caused by IRET instruction

                    All other bits are reserved for future use.

     argbytes       The total number of bytes used by PROC arguments.

     localbytes     The total number of bytes used by variables defined
                    with the LOCAL directive inside the PROC block.

     reglist        Comma-delimited list of registers from the PROC
                    directive USE option. This list is reversed in
                    in the EPILOGUE directive. Angle brackets are
                    passed.

     userparms      Passed the optional macroarglist from the PROC
                    directive.
.context @NoKeyword
.context NoKeyword
..index keyword recognition
..index NOKEYWORD option
.topic Disable/Enable Keyword Recognition
.freeze 3
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION NOKEYWORD:<keywordlist>

  \bSee also:\p \aMASM 5.1 Compatibility\v@510out\v

  \bDescription:\p

     Disables the recognition of the reserved words in <keywordlist>
     for the current module. The listed keywords can then be used as
     user-defined symbols. Keywords disabled with this directive cannot
     be reenabled. The <keywordlist> is a space-delimited list of
     reserved words; literal operands and predefined symbols are not
     allowed. The list of keywords must be enclosed in \aangle brackets (<>)\v@anglebrackets\v.

     You can use this option to disable privileged instruction names
     or obsolete synonyms so they are free to be used as labels and
     variables. For example:

               OPTION  NOKEYWORD: <STR EXTRN IRPC>
          STR  BYTE    10 DUP (?)

     MASM 6.1 has several new keywords that MASM 5.1 does not recognize as
     reserved. To resolve any conflicts, you can rename any offending symbols
     in your code, or selectively disable keywords with the OPTION NOKEYWORD
     directive. 

     The second option lets you retain the offending symbol names in your code
     by forcing MASM 6.1 to not recognize them as keywords. For example,

          OPTION NOKEYWORD:<INVOKE STRUCT>

     removes the keywords INVOKE and STRUCT from the assembler's list of
     reserved words. However, you cannot then use the keywords in their
     intended function, since the assembler will no longer recognize them.
.context @expr1632
.context EXPR16
.context EXPR32
..index 32-bit expressions, EXPR32
..index 16-bit expressions, EXPR16
..index EXPR16 option, Select 16-Bit Expressions
..index EXPR32 option, Select 32-Bit Expressions
.topic Select 16- or 32-Bit Expressions
.freeze 3
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION EXPR16

            OPTION EXPR32

  \bSee also:\p HIGHWORD, LOWWORD, \aOPTION M510\v@M510\v

  \bDescription:\p

     The EXPR32 option selects 32-bit expressions and arithmetic. The
     EXPR16 option selects 16-bit expressions and arithmetic. The
     default is EXPR32. The expression word size cannot be changed once
     it has been explicitly set.

     These options do not affect the processor type, segment size, or
     registers used.

     Under OPTION M510, EXPR16 is the default unless the module contains
     a .386 or greater processor-selection directive.

     MASM 5.1 treats expressions as 16-bit words if you do not specify .386 or
     .386P directives. MASM 6.1 by default treats expressions as 32-bit words
     regardless of the CPU type. You can force MASM 6.1 to use the smaller
     expression size with the OPTION EXPR16 statement.

     Unless your MASM 5.1 code specifies .386 or .386P, OPTION M510 also sets
     16-bit expression size. You can selectively disable this by following
     OPTION M510 with the OPTION EXPR32 directive, which sets the size back to
     32 bits. You cannot have both OPTION EXPR32 and OPTION EXPR16 in your
     program.

     It may not be easy to determine the effect of changing from 16-bit inter-
     nal expression size to 32-bit size. In most cases, the 32-bit word size
     does not affect the MASM 5.1 code. However, problems may arise because
     of differences in intermediate values during evaluation of expressions.
     You can compare the files for differences by generating listing files
     with the /Fl and /Sa command-line options with and without OPTION EXPR16
.context @optseg
.context SEGMENT:
..index SEGMENT option
..index USE16
..index USE32
..index FLAT
.topic Select Segment Size
.freeze 3
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION SEGMENT:segmentsize

  \bSee also:\p SEGMENT, .MODEL, \aTable of Memory Models\v@memorymodels\v

  \bDescription:\p

     The SEGMENT:USE16 option selects 16-bit segments. The SEGMENT:USE32
     option selects 32-bit segments. The SEGMENT:FLAT option selects
     the 32-bit flat segment. This option overrides the segment size set
     optionally set by the SEGMENT and .MODEL directives.

     The default segment size is selected by the following rules:

     ş A .386 or higher processor selection directive selects USE32.

     ş A .MODEL FLAT statement selects FLAT. All other memory models select
       USE16.

     ş The OPTION SEGMENT: directive will override any other defaults.

     ş In all other cases, the default is USE16.
.context @nosignextend
.context nosignextend
..index NEC V-series
..index V25/V35 processors
..index NOSIGNEXTEND option
..index sign extension,, disabling
.topic Disable Byte Sign-Extended Opcode
.freeze 3
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION NOSIGNEXTEND

  \bSee also:\p AND, OR, XOR

  \bDescription:\p

     The NOSIGNEXTEND option disables the generation of the sign-
     extended form (83h) of the AND, OR, and XOR instructions. The NEC
     V25 and V35 microprocessors do not support this opcode.
.context @optproc
..index PRIVATE
..index PUBLIC
..index EXPORT
..index PROC option
..index visibility,, setting PROC default
.topic Select Default PROC Visibility
.freeze 3
                                             \i\p\aUp\v@Option\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPTION PROC:visibility

  \bSee also:\p PROC

  \bDescription:\p

  Sets the default procedure visibilty. The <visibility> parameter can
  be PRIVATE, PUBLIC, or EXPORT. If you do not use this option, the
  the default visibility will be PUBLIC.

  With OPTION M510, the default <visibility> with no defined language type
  is PRIVATE. If you specify a language type in a .MODEL or OPTION LANGUAGE
  statement, the default <visibility> is PUBLIC.

     Without a language type, MASM 5.1 makes procedures private to the
      module. By default, MASM 6.1 makes procedures public. You can explicitly
     change the default visibility to private with either OPTION M510,
     OPTION PROC:PRIVATE, or OPTION PROC:EXPORT.

     To selectively enable MASM 6.1 behavior, place the directive
     OPTION PROC:PUBLIC after OPTION M510. You can override the default
     by adding the PUBLIC or PRIVATE keyword to selected procedures.
.context ORG
.context @ORG
.topic Specify Assembly Start Address
.freeze 3
..index ORG directive
 \i\p\aExample\v@helloc.asm\v\i\p                                   \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   ORG expression

            ORG $+expression

  \bSee also:\p ALIGN, EVEN, SEGMENT, STRUCT, \aData-Declaration Directives\v@byte\v

  \bDescription:\p

     Sets the location counter to a specific address, given by <expression>.
     The assembler implements this directive by adjusting the
     location counter.

     ORG $+expression causes the assembler add <expression> bytes to
     the location counter. The <expression> must evaluate to a constant
     without forward references.

     Inside a STRUCT block, the <expression> parameter is relative to
     the beginning of the structure. This can be used to create
     structure elements that refer to bytes lower in memory than the
     beginning of the structure.
.context PAGE
.context @PAGE
.topic Set Page Size
.freeze 3
..index PAGE directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   PAGE [[length], width]

  \bSee also:\p \aPAGE +\v@PagePlus\v, TITLE, SUBTITLE, \a/S<x>\vml.hlp!/Sx\v

  \bDescription:\p

     Sets maximum number of lines per page (length) and maximum number
     of characters per line (width) for listing files. Generates a page
     break if no arguments are given. A zero for <width> means that the
     assembler does not generate line breaks. A zero for <length> means
     that the assembler does not generate page breaks.

     The <length> parameter is a number between 10 to 255 lines (default
     is 0). The <width> parameter is a number between 60 to 255
     characters (default is 0). To specify width without changing the
     default length, omit the length parameter and precede <width> with
     a comma.
.context @PagePlus
.context PAGE +
.context PAGE+
.topic Cause a Page Break
.freeze 3
..index PAGE + directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   PAGE +

  \bSee also:\p PAGE, TITLE, SUBTITLE

  \bDescription:\p

     Causes a page break by inserting an ASCII form-feed character
     (0Ch), increments the section number, and resets the page number
     to 1. This directive is useful only with printer listings.

     Program-listing page numbers use the following format:
     section-page, where section is the section number within the
     module, and page is the page number within the section. By
     default, section and page numbers begin with 1-1.
.context FORCEFRAME
.context LOADDS
.context ENDP
.context PROC
.context procedure
.context @PROC
.topic Define Procedure
..index declaring parameters
..index PROC directive
..index ENDP directive
..index PRIVATE visibility
..index PUBLIC visibility
..index EXPORT visibility
..index FORCEFRAME attribute
..index LOADDS attribute
.freeze 3
 \i\p\aExample\v@colors.asx\v\i\p                                   \i\p\aUp\v@Labeling\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   label PROC [distance] [langtype] [visibility]
            [<prologuearg>]
            [USES reglist] [,parameter [:tag]]...
                    [LOCAL varlist]
                    statements
            label ENDP

  \bSee also:\p \aProcedure Diagram\v@proccommdiag\v, USES, INVOKE, PROTO, LOCAL, RET,
            .MODEL, \aOPTION PROLOGUE\v@prologue\v, \aOPTION PROC\v@optproc\v,
            \aUsing VARARG in Procedures\v@VARARG.proc\v, \aMASM 5.1 Compatibility\v@510procoff\v

  \bDescription:\p

     Defines a procedure called <label>. To use <parameter>, you must
     have previously specified a language type or used the <langtype>
     parameter. You should use a PROTO prototype before each procedure
     defined with the PROC directive. This ensures proper type checking
     and allows you to call the procedure with the INVOKE directive.

     Procedures usually end with the RET instruction. You can remove
     data from the stack by specifying the number of bytes as a
     parameter of the RET instruction.

     The assembler automatically generates PROLOGUE and EPILOGUE code
     to pass stack arguments properly to the procedure and clean up the
     stack after completion of the procedure. You can override the
     default prologue and epilogue code with user-defined macros by
     using the \aOPTION PROLOGUE: and OPTION EPILOGUE:\v@PROLOGUE\v directives.
     The RETF or IRETF instructions do not cause the assembler to
     generate epilogue code.

     Procedures can be nested if they do not have parameters, local
     variables or the USES parameter and do not generate a new segment
     or group. You can avoid having to nest procedures by using the RETN
     and RETF instructions.

     Local variables must be declared (using the LOCAL directive)
     before any instructions. There can be several LOCAL statements in
     a procedure.
     See: \i\p\aLOCAL directive\v@LOCAL.proc\v\i\p

     \bParameter\p      \bDescription\p

     label          Defines name for the procedure. Follows naming
                    convention of <langtype>.

     prologuearg    Arguments to pass to the prologue procedure. With
                    the default PROLOGUE and EPILOGUE, the FORCEFRAME
                    argument generates a stack segment, even if one is
                    not necessary. LOADDS causes the DS register to be
                    saved in the PROLOGUE and restored in the EPILOGUE.
                    Parameters must be separated by commas. Angle brackets
                    are required but not passed.

     distance       NEAR, FAR, NEAR16, NEAR32, FAR16, or FAR32. Indicates
                    the call distance of this procedure. If you choose
                    NEAR or FAR, the assembler will select 16- or 32-bit
                    NEAR or FAR depending on the current segment size. If
                    you do not specify this option, the assembler
                    determines the distance from the memory model and
                    processor type. NEAR is the default if you do not use
                    the .MODEL directive.

     langtype       Any valid language type. Determines naming style
                    and calling convention.
                    See: \i\p\alanguage type\v@langtype\v\i\p

     visibility     PRIVATE, PUBLIC, or EXPORT. Determines how the
                    procedure is made available to other modules. PUBLIC
                    is the standard default, but the default can be reset
                    with the \aOPTION PROC\v@optproc\v directive. EXPORT implies PUBLIC
                    and FAR and informs the linker that the procedure
                    should be placed in the export entry table.

     reglist        List of registers that the prologue preserves on the
                    stack and the epilogue restores on exit. Separate
                    multiple registers with spaces.

     parameter      Procedure parameter. The assembler translates
                    argument references into a direct reference to the
                    stack location. Separate multiple arguments with
                    commas.

     tag            Either a qualified type or \aVARARG\v@vararg.proc\v. \aVARARG\v@vararg.proc\v allows a
                    variable number of arguments to be passed as a
                    comma separated list to <argument>. If \aVARARG\v@vararg.proc\v is
                    used, it must be applied to the last parameter of
                    the PROC directive. \aVARARG\v@vararg.proc\v is only allowed with
                    the C, SYSCALL, and STDCALL language types. If <tag>
                    is omitted, the assembler defaults to WORD in a 16-bit
                    segment or DWORD in a 32-bit segment.
                    See: \i\p\aqualified type\v@qualifiedtype\v\i\p, \i\p\aVARARG\v@VARARG.proc\v\i\p

     statements     Assembly-language statements and directives.
.context @proccommdiag
.freeze 3
.topic PROC/PROTO/INVOKE Diagram
                                             \i\p\aUp\v@EXTERNDEF\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     Procedures can be accessed across source files in the following
     manner:

                   ÚÄTOOLS.INCÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
                   ³                                ³
                   ³ Task      PROTO C FAR arg:WORD ³
                   ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
                                   ³
                  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
                  ³                                  ³
     ÚÄUSER.ASMÄÄÄÁÄÄÄÄÄÄÄÄÄÄ¿          ÚÄTOOLS.ASMÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³                       ³          ³                             ³
     ³ INCLUDE   TOOLS.INC   ³          ³ INCLUDE   TOOLS.INC         ³
     ³                       ³          ³                             ³
     ³ INVOKE    Task, 5     ³          ³ Task      PROC C FAR \\     ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ          ³             USES cx dx,     ³
                                        ³             arg:WORD        ³
                                        ³           ù                 ³
                                        ³           ù                 ³
                                        ³           ù                 ³
                                        ³           ret               ³
                                        ³ Task      ENDP              ³
                                        ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

     NOTE: The PROC and INVOKE directives do not need to explicitly
           state the C and FAR attributes, because the PROTO directive
           sets them automatically.
.context PROTO
.context @PROTO
.topic Define a Procedure Prototype
.freeze 3
..index PROTO directive
 \i\p\aExample\v@hellom.asx\v\i\p                                   \i\p\aUp\v@Labeling\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   label PROTO [distance] [langtype] [, [parameter]:tag]...

  \bSee also:\p \aProcedure Diagram\v@proccommdiag\v, PROC, INVOKE, EXTERNDEF, TYPEDEF

  \bDescription:\p

     Defines a prototype for a subsequent procedure. Informs the
     assembler about how many and what kind of arguments should be
     expected in a procedure definition. This allows the assembler
     to perform type checking on arguments.

     If you define a prototype for all stack-based procedures defined
     with the PROC directive, you can place procedure prototypes in a
     separate include file. This is recommended when using procedures
     in a library file.

     \bParameter\p      \bDescription\p

     label          Defines name for the procedure. Must exactly match
                    <label> in PROC definition.

     distance       NEAR, FAR, NEAR16, NEAR32, FAR16, or FAR32.
                    Indicates the call distance of this procedure.
                    If you choose NEAR or FAR, the assembler will
                    select 16- or 32-bit NEAR and FAR depending on
                    the processor directive in effect. If you do not
                    specify this option, the assembler determines the
                    distance from the memory model and processor type.
                    NEAR is the default if you do not use the .MODEL
                    directive.

     langtype       Any valid language type. Determines naming style
                    and calling convention.
                    See: \i\p\alanguage type\v@langtype\v\i\p

     parameter      Procedure parameter. The assembler translates
                    argument references into a direct reference to the
                    stack location. Separate multiple arguments with
                    commas.

     tag            Either a qualified type or \aVARARG\v@vararg.proc\v. \aVARARG\v@vararg.proc\v
                    allows a variable number of arguments to be passed
                    as a comma separated list to <argument>. If \aVARARG\v@vararg.proc\v
                    is used, it must be applied to the last parameter
                    of the PROC directive. \aVARARG\v@vararg.proc\v is only allowed with
                    the C, SYSCALL, and STDCALL language types.
                    See: \i\p\aqualified type\v@qualifiedtype\v\i\p
.context PUBLIC
.context @PUBLIC
.topic Make Variable Public
.freeze 3
..index PUBLIC directive
 \i\p\aExample\v@hellomo.asm\v\i\p                                   \i\p\aUp\v@ScopeVisibility\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   PUBLIC [langtype] name [,[langtype] name]...

  \bSee also:\p \aOPTION PROC:PUBLIC\v@OPTPROC\v, EXTERN, EXTERNDEF, COMM, .MODEL,
            \aOPTION LANGUAGE:\v@LANG\v, SEGMENT, PROC, \a/C<x>\vml.hlp!/Cx\v, \a/H\vml.hlp!/H\v, \a/Zf\vml.hlp!/Zf\v,
            \aTable of Memory Models\v@MemoryModels\v, \aMASM 5.1 Compatibility\v@510public\v

  \bDescription:\p

     Makes one or more variables available to other modules in the
     program. PUBLIC names can be forward referenced.

     The following can be made PUBLIC:

        ş NEAR and FAR code labels and procedures
        ş Data created with user-defined types, BYTE, WORD, and other
          types
        ş Data created with user-defined structures, unions, and records
        ş Labels created with the LABEL directive
        ş Symbolic constants created with EQU or =

     The PUBLIC directive is normally used with the EXTERN directive.
     The EXTERNDEF and COMM directives allow more flexibility in
     global data declaration than the PUBLIC and EXTERN directives.
     One use is illustrated below.

                  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
                  ³                                  ³
     ÚÄUSER.ASMÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄ¿        ÚÄTOOLS.ASMÄÄÄÁÄÄÄÄÄÄÄÄÄÄ¿
     ³                        ³        ³                        ³
     ³ EXTERN global:WORD     ³        ³         PUBLIC  Global ³
     ³                        ³        ³ Global  WORD    5      ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ        ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

     \bParameter\p      \bDescription\p

     langtype       Any language type. Used to control what format
                    label names are sent to the linker.
                    See: \i\p\alanguage type\v@langtype\v\i\p

     name           The name of a variable, label, or numeric equate
                    defined within the current source file
.context PURGE
.context @PURGE
.topic Delete Macros from Memory
.freeze 3
..index PURGE directive
                                             \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   PURGE macroname [, macroname]...

  \bSee also:\p MACRO

  \bDescription:\p

     Deletes one or more currently defined macros from memory. Use this
     directive if your system does not have enough memory to keep all
     the macros needed for a source file in memory at the same time.
     The macro skeleton will remain, but it will not expand.

     A macro can remove itself if PURGE is on the last line of the
     macro. Redefining a macro automatically purges the previous
     definition. Calling a purged macro is legal, but the macro will
     act like a blank macro.
.context PUSHCONTEXT
.context ASSUMES
.context @PUSHCONTEXT
.context POPCONTEXT
.context LISTING
..index PUSHCONTEXT directive, Push Assembler Settings
..index POPCONTEXT directive, Pop Assembler Settings
.freeze 3
.topic Push/Pop Assembler Settings
                                             \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   PUSHCONTEXT context

            POPCONTEXT context

  \bSee also:\p ASSUME, RADIX, LIST, \aProcessor selection directive\v@ProcSelect\v

  \bDescription:\p

     PUSHCONTEXT stores the context information indicated in <context>.
     POPCONTEXT restores the context information indicated in <context>.
     You can nest PUSHCONTEXT directives up to 10 levels.

     You can use these directives to save and restore the state of the
     assembler in \ainclude files\v@include\v and \aprocedures\v@proc\v.

     \b<context>         Items Saved\p

     \aASSUMES           Register assumptions\v@ASSUME\v
     \aRADIX             Current default radix\v@.RADIX\v
     \aLISTING           Listing and cref flags\v@.LIST\v
     \aCPU               CPU and coprocessor directives\v@ProcSelect\v
     ALL               All of the above
.context .RADIX
.context @.RADIX
.context RADIX
.topic Set Default Radix (Number Base)
.freeze 3
..index .RADIX directive
                                             \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .RADIX expression

  \bSee also:\p \aRadix Specifiers\v@RadixSpec\v, PUSHCONTEXT, \aMASM 5.1 Compatibility\v@510radix\v

  \bDescription:\p

     Sets the default radix (base) for integer constants to the value
     of <expression>. The default radix at the start of assembly is 10.

     The .RADIX directive does not affect floating-point constants or
     numbers that have explicit radix overrride tags:

     \bTag\p        \bBase\p
     H          hexadecimal
     O or Q     octal
     T          decimal
     Y          binary
     R          floating point

     With a radix of 10 or below, you can use the characters B (b) and D
     (d) as the radix override characters for base 2 and base 10,
     respectively.

     The <expression> must evaluate to a decimal number in the range 2
     to 16 and cannot contain forward references.

     If the current radix in your code is greater than 10 decimal, MASM 6.1
     allows the radix specifiers B (binary) and D (decimal) only in 
     compatibility mode. You must change B to Y for binary, and D to T for
     decimal, since both B and D are legitimate hexadecimal values, making
     numbers such as 12D ambiguous. If you want to keep B and D as radix
     specifiers when the current radix is greater than 10, you must specify
     OPTION M510.
.context RECORD
.context {
.context }
.context @RECORD
.topic Declare Record
..index RECORD directive
..index braces, Declare Record ({}) Operator
..index {} punctuation, Declare Record ({}) Operator
.freeze 3
 \i\p\aExample\v@struct.asm\v\i\p                                   \i\p\aUp\v@CplxTypes\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   recordname RECORD field [, field]...

  \bSee also:\p STRUCT, MASK, WIDTH, \aOPTION EXPR16/EXPR32\v@expr1632\v,
            \aControl-Flow Directives\v@DirectCF\v

  \bDescription:\p

     Declares a record template of one or more specified bit fields
     but does not allocate storage for the template. Separate multiple
     fields with a comma.

     Records can be used as constants or expressions. They are treated
     as a BYTE or WORD with 16-bit expressions and as a BYTE, WORD, or
     DWORD with 32-bit expressions. The sum of the widths for all fields
     must not exceed the expression width.

     Once a record template is declared, you can use it to define
     record variables or constants, in which each field not initialized
     assumes the default value. Records are defined with the following
     syntaxes:

       [name1]   recordname   {[initializer1] [, initializer2]...}
       [name1]   recordname   <[initializer1] [, initializer2]...>

     Use of <recordname> is similar to the other variable definition
     directives (such as BYTE and WORD). Curly braces or angle brackets
     are required even if no initializers are given.

     If a record constant appears as part of an expression in a control
     flow directive (such as .IF or .WHILE), you must use curly braces
     instead of angle brackets to enclose the fields.

     The assembler will treat the name of a record field without the
     MASK or WIDTH operators as the bit number of the first bit of the
     record field.

     \bParameter\p      \bDescription\p

     recordname     A unique symbolic name.

     field          Each field is in the form

                         fieldname:width[= expression]

                    where <width> is a constant giving the width of the
                    field (in bits) and <expression> optionally sets an
                    initial or default value.
.context REPT
.context REPEAT
.context @REPEAT
.topic Repeat Assembly of Block
.freeze 3
..index REPEAT directive
                                             \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   REPEAT expression
              statements
            ENDM

  \bSee also:\p \a.REPEAT\v@.repeat\v, FOR, FORC, WHILE, MACRO, \aMASM 5.1 Compatibility\v@510ampers\v

  \bDescription:\p

     Assembles a block of statements <expression> times. REPT is a
     synonym for REPEAT and is included for compatibility with previous
     versions of the assembler.



     The <expression> parameter must evaluate to a numeric constant. This
     constant is evaluated during the first pass. The <statements> are
     any valid assembler statements.
.context .REPEAT
.context @.REPEAT
.context .UNTIL
.context UNTIL
.context .UNTILCXZ
.context UNTILCXZ
.topic Code Repeat Blocks
.freeze 3
..index .REPEAT directive
..index .UNTIL[CXZ] directive
 \i\p\aExample\v@loops.asm\v\i\p                                   \i\p\aUp\v@DirectCF\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .REPEAT
              statements
            .UNTIL condition

            .REPEAT
              statements
            .UNTILCXZ [condition]

  \bSee also:\p REPEAT, .WHILE, .BREAK, .CONTINUE, .IF, \aLOOPE Instruction\v@Loope\v,
            \aLOOPNE Instruction\v@Loope\v, \aComparison Operators\v@CCompare\v

  \bDescription:\p

     Used for clear coding of common loop blocks. Generates code causing
     the processor to repeat the following block of <statements> until
     <condition> is true. The .UNTIL statement evaluates <condition> and
     jumps back to the .REPEAT statement if <condition> is not true
     (zero). The block can iterate one (if <condition> is false when the
     .UNTIL is first reached) or more times.

     .REPEAT statements can be nested, and the assembler will optimize
     to get the best possible code for evaluating the conditional. A
     .UNTIL 0 statement will cause the loop to iterate until it is
     exited with a .BREAK statement or a jump out of the loop body.

     The .UNTILCXZ directive can be used with expression1 ==
     expression2 or expression1 != expression2 to generate a LOOPE or
     LOOPNE instruction. The .UNTILCXZ directive also decrements CX and
     exits the loop if CX=0. If <condition> is omitted, the .UNTILCXZ
     directive generates a LOOP instruction.
.context MEMORY
.context AT
.context PARA
.context COMMON
.context SEGMENT
.context @SEGMENT
.topic Define Segment
.freeze 3
..index SEGMENT directive
..index MEMORY
..index AT
..index PARA
..index COMMON
..index PRIVATE
..index USE16
..index USE32
..index READONLY
 \i\p\aExample\v@hellomo.asm\v\i\p                                   \i\p\aUp\v@CompSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name SEGMENT [align] [READONLY] [combine] [use] ['class']
              statements
            name ENDS

  \bSee also:\p ENDS, ASSUME, GROUP, ALIGN, EVEN, .386, .DOSSEG, .MODEL,
            @CurSeg, \aOPTION READONLY\v@READONLY\v, \aOPTION SEGMENT\v@optseg\v

  \bDescription:\p

     Defines a program segment called <SegName> with optional segment
     attributes: align, READONLY, combine, use, and class. These
     attributes give the linker and assembler information on how to set
     up, combine, and refer to segments. A segment can be reopened within
     a module by specifying the same <name> in a later SEGMENT statement.

     The SEGMENT directive automatically saves any segment definition
     that is already in effect. When the assembler encounters a closing
     ENDS, it restores the saved segment definition.

     \bParameter\p      \bDescription\p

     name           Name of the segment being defined. You can use the
                    @CurSeg macro function with the ENDS directive to
                    terminate the current segment.

     align          BYTE, WORD (2 bytes), DWORD (4), PARA (16), or PAGE
                    (256). Aligns segment to boundary. PARA is the
                    default.

     combine        PRIVATE, PUBLIC, STACK, COMMON, MEMORY, or AT
                    address. Defines how the linker combines segments.
                    PRIVATE is the default.

     use            USE16, USE32, or FLAT. Selects the segment word size.
                    See: \i\p\aOPTION SEGMENT\v@optseg\v\i\p

     READONLY       Makes segment read-only. The assembler will generate
                    an error if an instruction explicitly modifies a
                    segment marked with the READONLY attribute. Useful
                    for protected-mode code segments and segments in ROM.

     class          A class name (such as CODE or DATA). Must be enclosed
                    in quotation marks. Used to associate segments with
                    different names and to control segment order. Code
                    segments should have class 'CODE'.
.context \@SizeStr
.context @SizeStr
.context SIZESTR
..index length of string, @SizeStr Macro
..index length of string, SIZESTR Directive
..index SIZESTR directive
..index @SizeStr macro function
.topic Find String Length
.freeze 3
                                             \i\p\aUp\v@StringOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @SizeStr(string)

            name SIZESTR textitem

  \bSee also:\p INSTR, SUBSTR, CATSTR, TEXTEQU, SIZE, \aAngle Brackets (<>)\v@anglebrackets\v,
            \aexpression operator (%)\v@percent\v

  \bDescription:\p

     The @SizeStr macro function returns the length of a text item in
     characters. The @SizeStr function will not expand macros or
     expressions unless you use the \aexpression operator (%)\v@percent\v. A zero
     indicates a null (empty) string.

     The SIZESTR directive performs the same function but assigns the
     result to <name>. These two lines are equivalent:

          namelength   TEXTEQU     @SizeStr(<Denise>)
          namelength   SIZESTR     <Denise>
                                   ;namelength is 6

     The <name> is a unique symbolic name, <string> is a string, and
     <textitem> must be a text item.
     See: \i\p\atext item\v@textitem\v\i\p
.context .STACK
.context @.STACK
.context STACK
.topic Start Stack Segment
.freeze 3
..index .STACK directive
 \i\p\aExample\v@hello.asm\v\i\p                                   \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .STACK [size]

  \bSee also:\p @stack, .MODEL, .DATA, \a.DATA?\v@.DATA\v, .FARDATA, \a.FARDATA?\v@.FARDATA\v,
            .CODE, .CONST, SEGMENT, \aTable of Memory Models\v@MemoryModels\v

  \bDescription:\p

     Creates the program stack segment (with segment name STACK) nested
     inside the current segment, if any. The .MODEL directive must
     precede this directive. The .MODEL directive generates a GROUP
     statement that places the STACK segment in DGROUP unless the
     FARSTACK attribute is given.

     A single .STACK statement opens and closes the stack segment,
     so you should not include an additional ENDS statement.

     The <size> parameter specifies size of the stack in bytes (default
     1024).
.context STRUCT
.context @STRUCT
.context STRUC
.topic Declare Structure Data Type
.freeze 3
.comment..index STRUC directive
..index STRUCT directive
 \i\p\aExample\v@struct.asm\v\i\p                                   \i\p\aUp\v@CplxTypes\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name STRUCT [alignment] [, NONUNIQUE]
              fielddeclarations
            name ENDS

  \bSee also:\p \aStructure field operator (.)\v@period\v, ENDS, UNION, RECORD, DUP, \a/Zp\vml.hlp!/Zp\v,
            \aOPTION OLDSTRUCTS\v@oldstructs\v, \aMASM 5.1 Compatibility\v@510struct\v

  \bDescription:\p

     Declares a structure as a new data type. STRUC is a synonym for
     STRUCT and is included for compatibility.

     Once a structure name is declared, you can use it to define
     variables in which each field not initialized assumes the default
     value:

        [name]  structname  <[initializer [, initializer]...]>
     or
        [name]  structname  { \\
                                [initializer] \\
                                [, initializer]... \\
                                               }
     or
        [name]  structname  constant DUP ({[initializer[, initializer]...]})

     Curly braces or angle brackets are required even if no initializers
     are given. The DUP operator can be used to make duplicate
     initializations. Structures may be nested.

     You must refer to structure members in the format
     structurename.membername or with a pointer to a structure followed
     by the .membername tag. You can use multiple sequential membernames
     to specify elements of nested structures.
     See: \i\p\aStructure field operator (.)\v@period\v\i\p, \i\p\aPTR\v@ptr\v\i\p

     The NONUNIQUE parameter requies elements of the structure to be
     accessed only by their fully specified names.

     The ORG directive has a special meaning inside a STRUCT block.
     See: \i\p\aORG directive\v@ORG\v\i\p

     \bParameter\p         \bDescription\p

     name              A unique symbolic name for the structure.

     alignment         1, 2, or 4. Byte boundary to align structure
                       fields. The default is 1.
                       See: \a/Zp\vml.hlp!/Zp\v

     fielddeclarations Any valid data definitions (using BYTE, WORD,
                       etc.) with initial values. Can also be another
                       previously defined structure or a user-defined
                       type. Field names do not need to be unique; you
                       can reuse the same name in multiple structures.
                       Some other directives are allowed in this field;
                       see the BNF Appendix in the MASM Programmer's
                       Guide.

     initializer       Data initializer for structure field.
.context .STARTUP
.context .EXIT
.context @.STARTUP
.context STARTUP
.context EXIT
.topic Generate Start-Up/Exit Code
.freeze 3
..index .STARTUP directive
..index .EXIT directive
 \i\p\aExample\v@hello.asm\v\i\p                                   \i\p\aUp\v@SimpleSeg\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .STARTUP

            .EXIT [expression]

  \bSee also:\p .MODEL, END, ORG, INCLUDELIB, \a@Exit DOS Macro\v@exit\v,
            \aTable of Memory Models\v@MemoryModels\v


     The .STARTUP directive generates start-up code for the given CPU
     type and the memory model, operating system, and stack type
     defined by the .MODEL directive. Defines a start-address label,
     so that you don't need to give a start address with END. .STARTUP
     initializes DS, SS, and SP under DOS.

     The .EXIT directive generates code that terminates the program and
     returns control to the operating system or calling program. Use of
     the <expression> parameter returns a numeric exit code to the
     operating system or calling program.

     The <expression> parameter is an optional return code that can be
     read by the calling program or by a batch file. It can be a byte-
     sized register (word-sized under OS_OS2), memory location, or
     constant.

     With the TINY model, the .STARTUP directive also generates an
     ORG 100h.

     The .MODEL directive must precede these directives. The default
     OSType is DOS.

     To use the .EXIT directive under OS/2, you must link with a library
     that provides a termination procedure with a prototype.

.comment
.comment     To use the .EXIT directive under OS/2, you must link with OS2.LIB
.comment     or a library that provides a termination procedure with a
.comment     prototype such as:
.comment
.comment       DosExit  PROTO   terminate: WORD,   ; This is the prototype in
.comment                        exitcode:  WORD    ;  OS2.INC
.context \@SubStr
.context @SubStr
.context SUBSTR
..index strings,, extracting characters, SUBSTR Directive
..index strings,, extracting characters, @SubStr Macro
..index SUBSTR directive
..index @SubStr macro function
.topic Extract Characters from String
.freeze 3
 \i\p\aExample\v@ArgRev.asx\v\i\p                                   \i\p\aUp\v@StringOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   @SubStr(string, start [,length])

            name SUBSTR textitem, start [,length]

  \bSee also:\p INSTR, SIZESTR, CATSTR, TEXTEQU, \aAngle Brackets (<>)\v@anglebrackets\v,
            \aexpression operator (%)\v@percent\v.
  \bDescription:\p

     The @SubStr macro function extracts specified characters from a
     text item. If <length> is not given, the function will return all
     characters to the right of (and including) the character pointed
     to by <start>. The @SubStr function will not expand macros or
     expressions unless you use the \aexpression operator (%)\v@percent\v.

     The SUBSTR directive performs the same function but assigns the
     result to <name>. These two lines are equivalent:

       country    TEXTEQU     @SUBSTR (<AndorraMonacoLuxembourg>,8,6)
       country    SUBSTR      <AndorraMonacoLuxembourg>,8,6
                              ;country is <Monaco>

     \bParameter\p      \bDescription\p

     name           A unique symbolic name.

     string         A string to extract characters from.

     textitem       A text item to extract characters from.
                    See: \i\p\atext item\v@textitem\v\i\p

     start          Starting position of substring; the first character
                    is indexed as 1. Must be a positive integer constant.

     length         Number of characters to extract. Must be a positive
                    integer constant.
.context .TFCOND
.context @.TFCOND
.context TFCOND
.topic Toggle .LISTIF/.NOLISTIF
.freeze 3
..index .TFCOND directive
                                             \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .TFCOND

  \bSee also:\p .LISTALL, .LIST,  .NOLIST, .LISTIF, .NOLISTIF, .TFCOND,
            .LISTMACROALL, .LISTMACRO, .NOLISTMACRO

  \bDescription:\p

     Toggles the state of the assembler between .LISTIF and .NOLISTIF,
     determining whether or not false conditional blocks are included
     in assembler listings.
.context TITLE
.context SUBTTL
.context SUBTITLE
.context @TITLE
.topic Specify Title/Subtitle
.freeze 3
..index SUBTITLE directive
..index TITLE directive
 \i\p\aExample\v@hello.asm\v\i\p                                   \i\p\aUp\v@ListCtrl\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   TITLE text

            SUBTITLE text

  \bSee also:\p PAGE, \aPAGE +\v@PagePlus\v, \a/S<x>\vml.hlp!/Sx\v

  \bDescription:\p

     The TITLE directive specifies the title to be used on each page
     of assembly listings. The <text> is printed flush left on the
     second line of every page of the listing. Only one TITLE directive
     per module is allowed.

     The SUBTITLE directive specifies the subtitle to be used on each
     page of assembly listings. The <text> is printed flush left on the
     third line of every page of the listing. Each new SUBTITLE
     statement replaces the current subtitle; the change will be
     displayed on the next page of the output. SUBTTL is a synonym of
     SUBTITLE and is included for compatibility with previous versions
     of the assembler.

     The <text> is any combination of characters up to 60 characters.
.context TYPEDEF
.context @TYPEDEF
.topic Define Type Symbol
.freeze 3
..index TYPEDEF directive
 \i\p\aExample\v@data.asm\v\i\p                                    \i\p\aUp\v@CplxTypes\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name TYPEDEF qualifiedtype

            name TYPEDEF PROTO prototypelist

  \bSee also:\p \aTYPEDEF Uses\v@typedef.use\v, STRUCT, UNION, PROTO, PTR, \aqualified type\v@QualifiedType\v

  \bDescription:\p

     Defines a new type or creates a synonym for an already defined
     type. The TYPEDEF directive associates <name> with a type, and,
     once defined, the symbol can be used anywhere a type can be used
     or as a data declaration directive. The <qualifiedtype> parameter
     can specify a pointer to a qualified type. The TYPEDEF PROTO
     syntax defines a prototype type.

     Once a type is defined, you can use it to define variables. Use is
     similar to the other variable definition directives (like BYTE and
     WORD).

     \bParameter\p      \bDescription\p

     qualifiedtype     Any qualified type. Can include a type
                       previously defined by a TYPEDEF directive, but
                       recursive definitions are not allowed.
                       See: \i\p\aqualified type\v@qualifiedtype\v\i\p

     prototypelist     List of parameters for procedure prototype. Uses
                       the same syntax as the PROTO directive.
.context @TYPEDEF.USE
.freeze 3
.topic TYPEDEF Uses
                                             \i\p\aUp\v@CplxTypes\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSee also:\p \aTYPEDEF Directive\vTYPEDEF\v

  \bDescription:\p

     Use TYPEDEF to

        ş Define types for procedure arguments in PROC, PROTO, and
          INVOKE statements.

        ş Define types used in high-level expressions such as .IF,
          .WHILE, and .REPEAT.

        ş Define pointer types so that CodeView can give appropriate
          information about the pointer.

        ş Define the types for procedure arguments in procedure
          prototypes.

     Typical TYPEDEFs:

       CHAR       TYPEDEF   BYTE             ; Rename an existing type
       PCHAR      TYPEDEF   PTR   CHAR       ; Create a pointer type
       PWORD      TYPEDEF   PTR   WORD       ; Create a pointer type
       PPWORD     TYPEDEF   PTR   PWORD      ; Create a complex pointer
                                             ;  type
       PMYSTRUCT  TYPEDEF   PTR   MYSTRUCT   ; Create pointer type to
                                             ;  structure or union
       PPROTO     TYPEDEF   PROTO \\
                  NEAR C arg1:byte arg2:word ; Define proc prototype
       PFUNCT     TYPEDEF   PTR   PPROTO     ; Define pointer to proc
                                             ;  prototype
     Typical uses of TYPEDEFs:

       user       CHAR      "Jill"           ; Use new type as initializer
       Puser      PCHAR     user             ; Initialize a pointer
       task       PROC      myvar:PMYSTRUCT  ; Pass pointer to structure
                  EXTERNDEF ppw:PPWORD       ; Define external with user
                                             ;  type
       proctbl    PFUNC     Up, Down, Home   ; Initialize procedure table
                  ù
                  ù
                  ù
                  INVOKE    proctbl[bx], pos ; Call procedure from table
.context UNION
.context @UNION
.topic Declare Union as Data Type
.freeze 3
..index UNION directive
 \i\p\aExample\v@struct.asm\v\i\p                                   \i\p\aUp\v@CplxTypes\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name UNION [alignment] [, NONUNIQUE]
              fielddeclarations
            [name] ENDS

  \bSee also:\p ENDS, STRUCT, RECORD, \aDot Operator (.)\v@period\v, LABEL, \a/Zp\vml.hlp!/Zp\v,
            \aMASM 5.1 Compatibility\v@510union\v

  \bDescription:\p

     Declares a union as a new data type. Allows the same memory
     locations to be overlapped with different types. Identical to
     STRUCT except that each member has a 0 offset. The size of the
     union is the size of the largest member of the union.

     Unions can be used to store one type of data in some circumstances
     and another type in a different situation. Unions also allow you
     to store data as one data type and then read it as another. Unions
     are initialized by using the union name as a data declaration
     directive. You only need to initialize a union for one data type.

     You must refer to union members by their member names. The LABEL
     directive allows you to refer to the entire union as a unit.

     The first-level definition of a union must include the <name>
     label preceding the ENDS. In nested unions, terminate the block
     with a ENDS statement without the <name> label.

     The <alignment> field determines the byte alignment of any
     structures. This field can be 1, 2, or 4. The default is 1, unless
     overridden by the \a/Zp command-line option\vml.hlp!/Zp\v or the
     ALIGN or EVEN directives.

     The NONUNIQUE parameter allows elements of the union to be
     accessed only by their fully specified names.

     Some directives are allowed in the <fielddeclarations> field;
     see the BNF Appendix in the MASM Programmer's Guide.

  \bExample:\p

     EXTENDCHAR  UNION
                 ascii       BYTE    ?      ;Define a union of byte and
                 kanji       WORD    ?      ; word
     EXTENDCHAR  ENDS
     initial     EXTENDCHAR  <J>            ;Create a union variable
     mov         al, initial.ascii          ;Move byte-length data
     mov         bx, initial.kanji          ;Move word-length data
.context USES
.context @USES
..index USES directive, Save Registers Used in Procedure
.topic Save Registers Used in a Procedure
.freeze 3
 \i\p\aExample\v@FindFirs.asx\v\i\p                                   \i\p\aUp\v@Proc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   USES reglist

  \bSee also:\p PROC, RET

  \bDescription:\p

     An optional keyword used with PROC. Generates code to push the
     value of registers that should be preserved (and that will be
     altered by your procedure) on the stack and pop them off when the
     procedure returns.

     The <reglist> parameter is a list of one or more registers. Separate
     multiple registers with spaces.
.context WHILE
.context @WHILE
.topic Assemble While Expression is True
.freeze 3
..index WHILE directive
                                             \i\p\aUp\v@MacroIter\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   WHILE expression
              statements
            ENDM

  \bSee also:\p \a.WHILE\v@.while\v, FOR, FORC, REPEAT, ENDM, \aMASM 5.1 Compatibility\v@510ampers\v

  \bDescription:\p

     Marks a block of statements to be assembled repeatedly until
     <expression> is false (zero). The <expression> is a constant
     expression whose value is computed at assembly time.
.context .WHILE
.context @.WHILE
.context .ENDW
..index .WHILE directive
..index .ENDW directive, End WHILE Blocks
.freeze 3
.topic Code WHILE Blocks
 \i\p\aExample\v@loops.asm\v\i\p                                   \i\p\aUp\v@DirectCF\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   .WHILE condition
              statements
            .ENDW

  \bSee also:\p WHILE, .REPEAT, .BREAK, .CONTINUE, .IF, \aComparison Operators\v@CCompare\v

  \bDescription:\p

     Used for clear coding of common loop blocks. Generates code so
     that, while <condition> is true, the processor loops through the
     following block of statements.

     The .ENDW statement marks the end of the <statements> block,
     causing the processor to return to the .WHILE directive to
     reevaluate whether <condition> is still true. If <condition> is
     initially false, the <statements> will not be executed.

     .WHILE blocks can be nested, and the assembler will optimize to get
     the best possible code.
.context @colon
.context ::
..index :/:: punctuation
..index colon
..index double colon
..index :macrolabel
.freeze 3
.topic Labeling Punctuation (:/::)
                                             \i\p\aUp\v@AddressOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   label:

            label::

            :macrolabel

            register::register

  \bSee also:\p \aSegment Override (:)\v@op.Colon\v, \aLOCAL\v@local.proc\v, \a$\v@dollarsign\v, \aOPTION SCOPED\v@SCOPED\v, PUBLIC,
            GOTO, LABEL, INVOKE

  \bDescription:\p

     Gives name <label> to the memory location of the next instruction
     or piece of data. Can be target of loop, branch, jump, load, or
     any instruction or directive that requires a memory location.

     The register::register syntax is independent of this definition. In
     this case, the double-colon is used as a delimiter for a register
     pair passed with the INVOKE directive.

     With the single colon, <label> has global scope within its
     module under the default \aOPTION NOSCOPED\v@scoped\v. Under \aOPTION SCOPED\v@scoped\v,
     <label> is LOCAL inside a PROC block and is global elsewhere.
     The <label> cannot be declared public.

     The double colon acts the same as the single colon except that
     <label> has global scope within its module, independent of \aOPTION\v@scoped\v
     \aSCOPED\v@scoped\v, and can be declared PUBLIC.

     The :macrolabel labeling syntax is used exclusively inside MACRO,
     FOR, FORC, REPEAT, and WHILE blocks. It can be used only as the
     destination of the GOTO directive.
.context @op.colon
.context :
..index : operator
..index colon
.freeze 3
.topic Segment Override Operator (:)
 \i\p\aExample\v@WinClose.asx\v\i\p                                   \i\p\aUp\v@AddressOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   segment:expression

  \bSee also:\p SEG, SEGMENT, GROUP, ASSUME, \aLabeling (:/::)\v@colon\v

  \bDescription:\p

     Overrides the default segment of <expression> with <segment>.
     Forces the address of the variable or label to be computed
     relative to the specified segment.

     \bParameter\p      \bDescription\p

     segment        A segment register (CS, DS, SS, ES, FS, or GS),
                    group name, segment name, or segment expression.

     expression     A memory expression.
.context @period
.context .
..index . operator, Structure Field Reference Operator (.)
..index dot operator
.freeze 3
.topic Structure Field Reference (.)
                                             \i\p\aUp\v@ArithOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   expression.field[.field]...

            expression2.structtype.field[.field]...

  \bSee also:\p STRUCT, \aOPTION DOTNAME\v@DOTNAME\v, \aOPTION OLDSTRUCTS\v@OLDSTRUCTS\v, UNION, RECORD,
            \aMASM 5.1 Compatibility\v@510period\v

  \bDescription:\p

     Returns the offset of <expression> plus the value of <field>. This
     operator is usually used to access a field of data within a structure
     or union variable. You can use several dot operators to access nested
     structures.

     \bParameter\p      \bDescription\p

     expression1    An expression for which the qualified type is a
                    structure

     expression2    An arbitrary expression.
     
     structtype     A defined structure type.

     field          A field within a previously defined structure type.
                    Each field has an offset relative to the beginning
                    of the structure.

  \bExample:\p

     DPSTRUCT   STRUCT                          ; Memory-mapped dsp
                ctrlreg         WORD    ?       ;  data structure
                addrreg         DWORD   ?
     DPSTRUCT   ENDS

     IOSTRUCT   STRUCT                          ; Set up nested structure
                vio             VIDEOSTRUCT <>  ;  for I/O
                dsp             DPSTRUCT <>     ;
     IOSTRUCT   ENDS
     
     ioInst     IOSTRUCT        <>              ; Instance of IOSTRUCT

     lea        ax, (IOSTRUCT PTR [bx]).\\       ; Loads offset of
                dsp.ctrlreg                     ;  ctrlreg element of I/O
                                                ;  structure pointed to
                                                ;  by the bx register

     assume     bx:PTR IOSTRUCT
     lea        ax, [bx].dsp.ctrlreg            ; Same as above code line
     
     mov        ioInst.dsp.ctrlreg, 10h         ; Set control register to 10
.context @brackets
.context []
.context [
.context ]
..index [] punctuation, Index Punctuation ([])
.freeze 3
.topic Index ([]) Operator
 \i\p\aExample\v@Data.asm\v\i\p                                   \i\p\aUp\v@AddressOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   expression1[expression2]

  \bSee also:\p \aPlus (+)\v@ArithOp\v, \aDot Operator (.)\v@period\v

  \bDescription:\p

     The brackets (also called the "index operator") return the sum of
     <expression1> and <expression2>. If <expression2> is a register,
     the brackets indicate an indirect memory operand rather than a
     register-direct operand.

     In the syntax above, the brackets are literal and must be
     included. The expression outside the brackets is optional. You
     cannot add two direct memory operands.
.context @quotes
.context "
.context '
..index quote delimiter
.freeze 3
.topic String Delimiters ("/')
 \i\p\aExample\v@Data.asm\v\i\p                                    \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   "text"

            'text'

  \bSee also:\p \aAngle Brackets (<>)\v@anglebrackets\v, \aText Item\v@textitem\v

  \bDescription:\p

     Treats <text> as a string. You can use single or double quotation
     marks, but the quotation marks on both sides of <text> must be
     identical. A string can be up to 255 characters long and must be
     on a single physical line. You can use a quotation mark inside a
     string by repeating it twice in a row ("" or ''). The left-hand
     single quotation mark (`) is not allowed as a delimiter.

  \bExample:\p

     quote1      BYTE     "This was called by 'int 23h'"
     quote2      BYTE     'This was called by "int 23h"'
     quote3      BYTE     "Use the ""ASSUME"" directive"
     quote4      BYTE     'Use the ''ASSUME'' directive'
.context anglebrackets
.context @anglebrackets
.context <>
.context >
.context <
..index <> punctuation, Literal Text Punctuation (<>)
.freeze 3
.topic Literal Text Operator (<>)
 \i\p\aExample\v@ArgRev.asx\v\i\p                                   \i\p\aUp\v@MacroOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   <text>

  \bSee also:\p \aGreater Than/Less Than (</>)\v@ccompare\v, EQU, TEXTEQU, FOR,
            FORC, \aEscape Character (!)\v@exclamation\v, \aQuotation Marks\v@quotes\v, \aText Item\v@textitem\v

  \bDescription:\p

     Treats <text> as a single literal string.

     Angle brackets are often used in macro calls and with the FOR
     directive to ensure that values in a parameter list are treated as
     a single parameter. For example,

       MyMacro   <one, two, three>       ;Passes one argument to
                                         ;MyMacro

       MyMacro   one, two, three         ;Passes three arguments to
                                         ;MyMacro

     The assembler removes one set of angle brackets each time it
     inserts an argument into a macro expansion.

     The <text> is any name, number, or expression. It can contain
     commas, spaces, tabs, or semicolons. The exclamation point (!) is
     an escape character that allows you to include special characters
     such as the semicolon, ampersand, and quotation marks.
.context @exclamation
..Removed due to ! problems in parser context ! --JCK
..index ! operator
.freeze 3
.topic Literal Character Operator (!)
 \i\p\aExample\v@ArgRev.asx\v\i\p                                   \i\p\aUp\v@MacroOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   !character

  \bSee also:\p \aLogical NOT (!)\v@ccompare\v, \aAngle Brackets (<>)\v@anglebrackets\v, \aQuotation Marks ("/')\v@quotes\v

  \bDescription:\p

     Treats <character> as a literal character rather than as an
     operator or a symbol. Used with special characters (<, >, ", ', %,
     ;, and comma) when the usual assembly language meaning of the
     character must be suppressed. A double exclamation point (!!) is
     treated as a single exclamation point. The escape character does
     not apply to strings in quotation marks or to backslashes at the
     end of a line.

     For example:

      <I!'m afraid I can!'t do that!!>      I'm afraid I can't do that!
      <20!% !> 15!%>                        20% > 15%
.context @semicolon
.context ;
..index ;/;; punctuation
..index semicolon
..index double semicolon
.context ;;
.freeze 3
.topic Comment Delimiter (;/;;)
 \i\p\aExample\v@LoadPtr.asx\v\i\p                                   \i\p\aUp\v@MacroOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   ;text
            ;;text

  \bSee also:\p COMMENT

  \bDescription:\p

     The single semicolon treats <text> to the end of the line as a
     comment. The double semicolon treats <text> to the end of the
     line as a macro comment. In a listing file, macro comments appear
     with the macro definition, not with the macro expansion.

     Comments must follow all other fields in a statement line.
.context @percent
.context %
..index % operator
..index percent sign
.freeze 3
.topic Expression Operator (%)
 \i\p\aExample\v@ArgRev.asx\v\i\p                                   \i\p\aUp\v@MacroOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   %expression

  \bSee also:\p \aSubstitution Operator (&)\v@ampersand\v, \aMASM 5.1 Compatibility\v@510out\v

  \bDescription:\p

     Computes the value of <expression> and treats it as a text string.
     Use in macro calls when you need to pass the result of an
     expression rather than the actual expression to a macro. The
     <expression> can also be a text macro or a macro function.

     When placed at the beginning of the line, % causes the
     calculation and substitution of all text macros on that line,
     including those inside \aangle brackets (<>)\v@anglebrackets\v. Macro function calls
     inside \aangle brackets (<>)\v@anglebrackets\v are not expanded unless preceded by a
     %.

     MASM 5.1 allows % as an identifier character. This behavior leads to
     ambiguities when % is used as the expansion operator in macros. Since %
     is not allowed as a character in MASM 6.1 identifiers, you must change
     the names of any identifiers containing the % character.
.context @ampersand
.context &
..index & operator
.freeze 3
.topic Substitution Operator (&)
                                             \i\p\aUp\v@MacroOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   &parameter&

  \bSee also:\p \aBit Test Operator (&)\v@ccompare\v, \aExpression Operator (%)\v@percent\v,
            \aEscape Character (!)\v@exclamation\v, \aMASM 5.1 Compatibility\v@510ampers\v

  \bDescription:\p

     Operator used within a macro or a repeat block; enables parameter
     substitution for <parameter>. Two parameters can be joined to form
     one symbol. You can join any number of parameters with the
     ampersand operator.

     You can also use this operator to force parameter substitution in
     a quoted string. For this reason, if you want an ampersand to be
     included in a string, you must use the \astring escape character (!)\v@ampersand\v
     before the &.

     Use ampersands (&) only around \amacro parameters\v@macro\v, in \aquoted strings\v@quotes\v,
     and inside \aangle brackets (<>)\v@anglebrackets\v. Each parameter to be substituted
     must have one ampersand on the left and right. If an ampersand is
     adjacent to white space, it can be omitted as long as one
     ampersand is next to each parameter to be substituted.

  \bExample:\p

     Guard    MACRO     Prisonernum:REQ, Prisonername:=Anonymous
                        ECHO    <Number &Prisonernum has arrived at &@Time.>
              EXITM     Prisonernum&&Prisonername
              ENDM

     Badge    Guard     2
.context @DUP
.context DUP
..index DUP operator
.freeze 3
.topic Repeat Declaration Operator
 \i\p\aExample\v@data.asm\v\i\p                                   \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   count DUP (initialvalue [,initialvalue]...)

  \bSee also:\p \aData Declaration Directives\v@byte\v, \aUndeclared Data Operator (?)\v@questionmark\v

  \bDescription:\p

     Specifies <count> declarations of <initialvalue>. You can define
     arrays, buffers, and other data structures consisting of multiple
     data objects of the same size with the DUP operator. The
     <initialvalue> parameters must be inside parentheses. This
     operator can be used with data definition directives.

     The <count> parameter is the number of declarations of
     <initialvalue>. This parameter cannot be negative or
     forward-referenced. The <initialvalue> parameter is either the
     undefined data symbol (?), a string, a constant expression, or
     another DUP operator. The value or values of <initialvalue> are
     evaluated once before duplication. Separate multiple initial
     values with commas.
.context @HIGH
.context HIGH
.context LOW
..index HIGH operator
..index LOW operator
.topic High/Low-Order Byte
.freeze 3
                                             \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   HIGH expression
            LOW expression

  \bSee also:\p HIGHWORD, LOWWORD, OFFSET

  \bDescription:\p

     The HIGH operator returns the high-order (most significant) byte
     of <expression> in two's-complement form. The LOW operator returns
     the low-order (least significant) byte of <expression> in two's-
     complement form. The <expression> must resolve to a constant or
     an OFFSET expression.
.context @HIGHWORD
.context HIGHWORD
.context LOWWORD
..index HIGHWORD operator
..index LOWWORD operator
.topic High/Low-Order Word
.freeze 3
                                             \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   HIGHWORD expression
            LOWWORD expression

  \bSee also:\p HIGH, LOW, OFFSET

  \bDescription:\p

     The HIGHWORD operator returns the high-order (most significant)
     word of <expression> in two's-complement form. The LOWWORD
     operator returns the low-order (least significant) word of
     <expression> in two's- complement form. The <expression> must
     resolve to a constant or an OFFSET expression. These are similar
     to the HIGH and LOW operators of MASM 5.1 except that
     HIGHWORD and LOWWORD can take only constants as operands,
     not relocatables (labels).
.context @LENGTH
.context LENGTH
.context @SIZE
.context SIZE
.context SIZEOF
.context LENGTHOF
..index SIZEOF operator, Return Variable Size
..index LENGTHOF directive,Return Variable Length
.topic Variable Length
.freeze 3
                                             \i\p\aUp\v@TypeSizeOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   LENGTHOF variable

            SIZEOF variable

            SIZEOF type

            LENGTH expression

            SIZE expression

  \bSee also:\p WIDTH, TYPE, SIZESTR, DUP, BYTE, MACRO,
            \aMASM 5.1 Compatibility\v@510lensiz\v

  \bDescription:\p

     The LENGTHOF operator returns the number of data items allocated
     for <variable>. The SIZEOF operator returns the total number of
     bytes allocated for <variable> or the size of <type> in bytes. For
     variables, SIZEOF is equal to the value of LENGTHOF times the
     number of bytes in each element.

     The LENGTH and SIZE operators are allowed for compatibility with
     previous versions of the assembler. When applied to a data label,
     the LENGTH operator returns the number of elements created by the
     DUP operator; otherwise it returns 1. When applied to a data
     label, the SIZE operator returns the number of bytes allocated by
     the first initializer at the <variable> label.

     In MASM 5.1, the LENGTH operator, when applied to a record type, returns
     the total number of bits in a record definition. In MASM 6.1, the state-
     ment LENGTH <recordName> returns error A2143. MASM 6.1 uses the
     SIZEOF operator to return information about records.

     With OPTION M510, you can apply the LENGTH and SIZE operators to any
     label. For a code label, SIZE returns a value of 0FFFFh for NEAR and
     0FFFEh for FAR. LENGTH always returns a value of 1. For strings, SIZE
     and LENGTH both return 1.

     Without OPTION M510, SIZE returns values of 0FF01h, 0FF02h, 0FF04h,
     0FF05h, and 0FF06h for SHORT, NEAR16, NEAR32, FAR16, and FAR32 labels,
     respectively. LENGTH returns 1 except when used with DUP, in which case
     it returns the outermost count. For arrays initialized with DUP, SIZE
     returns the length multiplied by the size of the type.

     The LENGTHOF and SIZEOF operators in MASM 6.1 handle arrays much more
     consistently. These operators return the number of data items and the
     number of bytes in an initializer.
.context @MASK
.context MASK
.topic Bit Mask
.freeze 3
..index MASK operator
 \i\p\aExample\v@struct.asm\v\i\p                                   \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   MASK record

            MASK recordfieldname

  \bSee also:\p WIDTH, NOT, RECORD

  \bDescription:\p

     Returns a bit mask for the bit positions in a record or record
     field. A bit in the mask contains a 1 if that bit corresponds to
     a field bit. All other bits contain 0.

     The <record> parameter is the name of any record. The
     <recordfieldname> parameter is the name of any field in a record.
.context OFFSET
.context @OFFSET
..index OFFSET directive
.freeze 3
.topic Offset Address
 \i\p\aExample\v@hello.asm\v\i\p                                   \i\p\aUp\v@AddressOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OFFSET expression

  \bSee also:\p \aOPTION OFFSET:\v@offset:\v, \aSegment Override (:)\v@colon\v, LROFFSET, SEG,
            \aMASM 5.1 Compatibility\v@510offoff\v

  \bDescription:\p

     Returns offset address of <expression> (usually a variable). The
     value returned by the OFFSET operator is an immediate operand.
     OFFSET can be applied to any immediate or memory expression
     except a register or segment.

     The offset returned is relative to either the current group
     or segment. If there is both a current group and a current
     segment, the offset will have a group-relative default offset.
     This default can be changed with the \aM510\v@M510\v and \aOFFSET: options\v@offset:\v.

     The OFFSET operator can accept an expression specifying a segment.
     In this case, the OFFSET operator returns the offset of the last
     byte of the given segment, relative to that segment's group.  This
     value reflects the size of the given segment when the OFFSET
     operator is assembled.

     The <expression> is any label, variable, segment, or other direct
     memory operand.
.context LROFFSET
.context @LROFFSET
..index LROFFSET operator
..index offset, Loader-resolved
..index loader-resolved offset
.topic Loader-Resolved Offset
..index Microsoft Windows OFFSET, LROFFSET operator
.freeze 3
                                             \i\p\aUp\v@AddressOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   LROFFSET expression

  \bSee also:\p OFFSET, \aSegment Override (:)\v@colon\v, SEG,
            \aMASM 5.1 Compatibility\v@510offoff\v

  \bDescription:\p

     Similar to OFFSET, but causes the offset to be resolved by the
     loader at run time. LROFFSET is usually applied to the offset of
     a function pointer. This run-time resolution allows Microsoft
     Windows to relocate code segments in real mode.

     The <expression> is any label, variable, or other direct memory
     operand.

  \bExample:\p

     INVOKE     CreateRoutine, LROFFSET MyFunc, 0
.context PTR
.context @PTR
..index PTR operator
.freeze 3
.topic Specify Type for Expression
 \i\p\aExample\v@AddLong.asx\v\i\p                                   \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   qualifiedtype PTR expression
            PTR qualifiedtype

  \bSee also:\p TYPE, THIS, \aqualified type\v@QualifiedType\v

  \bDescription:\p

     The first syntax treats <expression> as having the specified type.
     Type casting can be used with forward references or unlabeled
     addresses to specify a reference size, type, or distance.

     The second syntax is used in defining qualified types.
     See: \i\p\aqualified type\v@QualifiedType\v\i\p

     The PTR keyword can also appear in \aqualified types\v@qualifiedtype\v to indicate
     that the type is a pointer variable.

     The <qualifiedtype> can be any qualified type. The <expression>
     can be any valid expression. If <qualifiedtype> is a distance,
     <expression> cannot be a memory expresssion or a data label.
     See: \i\p\aqualified type\v@QualifiedType\v\i\p

     You can use a constant as the left operand to PTR in compatibility mode.
     Otherwise, you must use a type expression. With OPTION M510, a constant
     must have a value of 1 (BYTE), 2 (WORD), 4 (DWORD), 6 (FWORD), 
     8 (QWORD) or 10 (TBYTE). The assembler treats the constant as the
     parenthesized type. Note that the TYPE operator yields a type expression,
     but the SIZE operator yields a constant.

     ; With OPTION M510          

          MyData  DW      0          

             mov     WORD PTR [bx], 10           ; Legal          
             mov     (TYPE MyData) PTR [bx], 10  ; Legal          
             mov     (SIZE MyData) PTR [bx], 10  ; Legal          
             mov     2 ptr [bx], 10              ; Legal          

     ; Without OPTION M510          

          MyData  WORD    0          

             mov     WORD PTR [bx], 10           ; Legal          
             mov     (TYPE MyData) PTR [bx], 10  ; Legal          
     ;       mov     (SIZE MyData) PTR [bx], 10  ; Illegal          
     ;       mov     2 PTR [bx], 10              ; Illegal



  \bExample:\p

     mov        ax, (MyStruct PTR [bx]).Username
     fld        TBYTE PTR MaxConvolution
     INVOKE     MyRoutine, PTR TempValue

     Under MASM 5.1, applying the PTR operator to a data initializer de-
     termines the size of the data displayed by CodeView. You can still use
     PTR in this manner in MASM 6.1, but it does not affect CodeView typing.
     Defining pointers with the TYPEDEF directive allows CodeView to generate
     correct information.
.context @SEG
.context SEG
..index SEG operator
.freeze 3
.topic Return Segment or Group of Expression
 \i\p\aExample\v@WinClose.asx\v\i\p                                   \i\p\aUp\v@AddressOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   SEG expression

  \bSee also:\p SEGMENT, OFFSET, LABEL, GROUP, ASSUME,
            \aMASM 5.1 Compatibility\v@510segment\v

  \bDescription:\p

     Returns the segment or group of <expression>. If <expression> does
     not have an explicit segment or group, SEG returns the segment or
     group of the SEG expression itself. If the SEG expression has no
     segment or group, SEG returns the value of <expression>.

     The <expression> is any label, variable, group name, or other
     memory operand; it cannot be a constant.

     In MASM 5.1, the SEG operator returns a label's segment address unless
     the frame is explicitly specified, in which case it returns the segment
     address of the frame. A statement such as SEG DGROUP:var always returns
     DGROUP, whereas SEG var always returns the segment address of var.
     OPTION M510 forces this same behavior in MASM 6.1. If you do not use
     OPTION M510, the behavior of the SEG operator is determined by the
     OPTION OFFSET directive

     In MASM 6.1, the value returned by the SEG operator applied to a non-
     external variable depends on compatibility mode. Without OPTION M510,
     SEG returns the address of the frame (the segment, group, or the value
     assumed to the segment register) if one has been explicitly set. With
     OPTION M510, SEG returns the group if one has been specified. In the
     absence of a defined group, SEG returns the segment where the variable
     is defined.
.context SHORT
.context @SHORT
..index SHORT directive
.freeze 3
.topic Force Label Type to SHORT
                                             \i\p\aUp\v@AddressOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   SHORT label

  \bSee also:\p JMP, \aOPTION LJMP\v@LJMP\v

  \bDescription:\p

     Forces <label> type to SHORT. Normally, the assembler selects
     short jumps and branches when possible, but SHORT can be used to
     generate an error if a short jump to <label> is not possible. It
     disables any automatic conditional-jump lengthening (\aOPTION LJMP\v@LJMP\v).

     Short labels can be used in JMP instructions when the distance is
     less than 128 bytes from the start of the next instruction. This
     saves a byte of memory and reduces the number of clock cycles used.
.context THIS
.context @THIS
..index THIS directive
.freeze 3
.topic Specify Same Operand Type
                                             \i\p\aUp\v@AddressOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   THIS qualifiedtype

  \bSee also:\p EQU, \aEqual Sign (=)\v@equalsign\v, LABEL

  \bDescription:\p

     Returns an operand of \a<qualifiedtype>\v@qualifiedtype\v with offset and segment
     values equal to the current location-counter value. THIS can be
     used with EQU or = to create labels and variables. THIS NEAR is
     equivalent to $.

     These two lines are equivalent:

       tag1     EQU      THIS BYTE
       tag1     LABEL    BYTE

     The <qualifiedtype> can be any qualified type.
     See: \i\p\aqualified type\v@qualifiedtype\v\i\p
.context @TYPE
.context TYPE
..index TYPE directive
.freeze 3
.topic Return Expression Attributes
                                             \i\p\aUp\v@TypeSizeOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   TYPE expression

  \bSee also:\p \a.TYPE\v@OPATTR\v, SIZEOF, LENGTHOF, WIDTH, PTR, MACRO, STRUCT

  \bDescription:\p

     The TYPE operator returns a value and size and distance attributes
     appropriate to <expression>. You can use the constant returned by
     TYPE directly or use the attributes returned by TYPE. For example,
     you can use the returned attribute to specify a type for PTR.

     \b<expression>\p    \bReturns\p

     Variable        Number of bytes allocated for each element
     Structure       Total number of bytes in the structure
     Constant        0
     Code label      Distance
     Register        Size of register in bytes
.context @OPATTR
.context OPATTR
.context .TYPE
..index OPATTR directive, .TYPE
.freeze 3
.topic OPATTR Operator
 \i\p\aExample\v@pushc.asx\v\i\p                                   \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   OPATTR expression

            .TYPE expression

  \bSee also:\p TYPE, MACRO

  \bDescription:\p

     The OPATTR operator returns a one-word constant defining the mode
     and scope of expression. If <expression> is not valid or is forward-
     referenced, OPATTR returns a 0. If <expression> is valid, a
     nonrelocatable word is returned. The .TYPE operator returns only the
     low byte (bits 0-7) of the OPATTR operator and is included for
     compatibility with previous versions of the assembler.

     \bBit          Set If\p
     \bPosition     <expression>\p

     0            References a code label
     1            Is a memory expression or has a relocatable data
                  label
     2            Is an immediate expression
     3            Uses direct memory addressing
     4            Is a register expression
     5            References no undefined symbols and is without error
     6            Is an SS-relative memory expression
     7            References an external label
     8-10         Language type:

                  \bBits\p     \bLanguage\p

                  000      No language type
                  001      C
                  010      SYSCALL
                  011      STDCALL
                  100      Pascal
                  101      FORTRAN
                  110      Basic

     All other bits (11-15) are undefined and reserved for future expansion.
.context WIDTH
.context @WIDTH
..index WIDTH operator
.freeze 3
.topic Record/Record-Field Width
                                             \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   WIDTH {recordfieldname³record}

  \bSee also:\p MASK, SIZEOF, LENGTHOF, RECORD

  \bDescription:\p

     Returns width (in bits) allocated for a record or record field.

     The <recordfieldname> is the name of any field defined in any
     record. The <record> is the name of any defined record.
.context @at_F
.context F
.context \@@
.context \@F
.context \@B
..index @F operand
..index @B operand
..index @@ label
.freeze 3
.topic Local Code Labels
                                             \i\p\aUp\v@LangConst\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   [instruction] @F
                .
                .
                .
            @@: [statement]
                .
                .
                .
                [instruction] @B

  \bSee also:\p \aB Radix Override\v@RadixSpec\v, LABEL, \aColon(:)\v@colon\v

  \bDescription:\p

     The @@: label defines a local code label, which is in effect until
     the next instance of @@:. The @F and @B operands can be used in
     conditional and unconditional jump statements to jump to the next
     and previous @@: label respectively.

     The @@: label is useful for defining a nearby jump point where a
     full label is not appropriate.

  \bExample:\p

           cmp     ax, 18h
           jg      @F
           ù                  ;Less than or equal
           ù
           ù
     @@:                      ;Greater than


           mov     cx, 640    ;Set count

     @@:
           ù                  ;Loop statements
           ù
           ù
           loop    @B         ;Loop back
.context \\
.context @backslash
.freeze 3
..index \ punctuation, Line-Continuation (\) Punctuation
.topic Line-Continuation (\\) Operator
 \i\p\aExample\v@getdisks.asx\v\i\p                                   \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   text \\
            text

  \bDescription:\p

     Enables an assembly-language statement to continue on the next
     line, ignoring the line break. You can join any number of physical
     lines with this method. A backslash occurring inside a comment
     or string is not considered to be a line-continuation character.
     Logical lines cannot exceed a total of 512 characters.

     Comments can appear to the right of the \\. A backslash is not
     required on a line ending with a comma.
.context @equalSign
.context =
..index = directive
..index equal sign (=)
.freeze 3
.topic Redefinable Numeric Equate
 \i\p\aExample\v@argi.asx\v\i\p                                   \i\p\aUp\v@DirectMisc\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   name = expression

  \bSee also:\p EQU, EQ, \a==\v@CCompare\v

  \bDescription:\p

     Creates or redefines a constant symbol by assigning the numeric
     value of <expression> to <name>. This equate can be used in
     subsequent statements as a symbolic constant and can be redefined
     at any time.

     The <name> field must be either a unique name or one previously
     defined with =. The <expression> fieldis any valid constant
     expression. Resolved at definition by the assembler.
.context @dollarsign
.context $
..index $ value
..index dollar sign
.freeze 3
.topic Current Offset ($) Value
                                             \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   $

  \bSee also:\p LENGTHOF, THIS

  \bDescription:\p

     Equates to the current value of the location counter (current
     offset address). Equivalent to THIS NEAR.

  \bExample:\p

     title    BYTE     "Space Mutants II"
     level    WORD     0
     hiscore  BYTE     20 DUP (?)
     tablelen EQU      $-title             ;tablelen = 38

     In compatibility mode, the current address operator ($) applied to a
     structure returns the offset of the first byte of the structure. When
     OPTION M510 is not enabled, $ returns the offset of the current field in
     the structure.
.context ?
.context @questionmark
..index ? initializer
..index question mark initializer
..index uninitialized data
.freeze 3
.topic Uninitialized Value (?)
                                             \i\p\aUp\v@MiscOp\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

  \bSyntax:\p   ?

  \bSee also:\p \aData Declaration Directives\v@BYTE\v, \a.DATA?\v@.DATA\v, \a.FARDATA?\v@.FARDATA\v,
            \aMASM 5.1 Compatibility\v@510questionmark\v

  \bDescription:\p

     Used as an initializer in data declarations; indicates a value
     that the assembler allocates but does not initialize. Can be used
     alone or with DUP, as in the following examples:

     Extension  WORD     ?           ; Allocate 1 uninitialized word
     Initials   BYTE     ?,?,?       ; Allocate 3 uninitialized bytes
     Fullname   BYTE     20 DUP (?)  ; Allocate 20 uninitialized bytes

     To minimize the size of executable files, use the \a.DATA?\v@.DATA\v or \a.FARDATA?\v@.FARDATA\v
     directives to define an undefined data segment. Put undefined data
     in these segments.

     The assembler treats the ? initializer as either zero or as an unspecified
     value. In compatibility mode, however, the assembler always treats the ?
     initializer as zero unless it is used with the DUP operator. In this case
     the assembler allocates space, but does not initialize it with any value. 
.context PRIVATE
.freeze 3
.topic PRIVATE multiplex
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     ÚÄPRIVATEÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³                                ³
     ³ \i\p\aWith the PROC directive\v@PROC\v\i\p      ³
     ³ \i\p\aWith the PROC option\v@OPTPROC\v\i\p         ³
     ³ \i\p\aWith the SEGMENT directive\v@SEGMENT\v\i\p   ³
     ³                                ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
.context LOCAL
.freeze 3
.topic LOCAL multiplex
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     ÚÄLOCALÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³                                ³
     ³ \i\p\aIn PROC blocks\v@LOCAL.proc\v\i\p               ³
     ³ \i\p\aIn MACRO blocks\v@LOCAL.mac\v\i\p              ³
     ³                                ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
.context EXPORT
.freeze 3
.topic EXPORT multiplex
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     ÚÄEXPORTÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³                                ³
     ³ \i\p\aWith the PROC directive\v@PROC\v\i\p      ³
     ³ \i\p\aWith the PROC option\v@OPTPROC\v\i\p         ³
     ³                                ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
.context USE16
.context USE32
.freeze 3
.topic USE16/USE32 multiplex
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     ÚÄUSE16/USE32ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³                                ³
     ³ \i\p\aWith the SEGMENT directive\v@SEGMENT\v\i\p   ³
     ³ \i\p\aWith the SEGMENT option\v@OPTSEG\v\i\p      ³
     ³                                ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
.context FLAT
.freeze 3
.topic USE16/USE32 multiplex
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     ÚÄFLATÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³                                ³
     ³ \i\p\aDescription of FLAT\v@Flat\v\i\p          ³
     ³ \i\p\aWith the .MODEL directive\v@.MODEL\v\i\p    ³
     ³ \i\p\aWith the ASSUME directive\v@ASSUME\v\i\p    ³
     ³ \i\p\aWith the SEGMENT directive\v@SEGMENT\v\i\p   ³
     ³ \i\p\aWith the SEGMENT option\v@OPTSEG\v\i\p      ³
     ³                                ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
.context VARARG
.context :VARARG
.freeze 3
.topic VARARG multiplex
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     ÚÄVARARGÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³                                ³
     ³ \i\p\aIn PROC blocks\v@VARARG.proc\v\i\p               ³
     ³ \i\p\aIn MACRO blocks\v@VARARG.mac\v\i\p              ³
     ³                                ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
.context NONUNIQUE
.freeze 3
.topic NONUNIQUE multiplex
                                             \i\p\aUp\v@contents\v\i\p \i\p\aContents\vpwb.hlp!hv\v\i\p \i\p\aIndex\vpwb.hlp!vx\v\i\p \i\p\aBack\v!B\v\i\p
ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ

     ÚÄNONUNIQUEÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³                                ³
     ³ \i\p\aWith the STRUCT directive\v@STRUCT\v\i\p    ³
     ³ \i\p\aWith the UNION directive\v@UNION\v\i\p     ³
     ³                                ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
